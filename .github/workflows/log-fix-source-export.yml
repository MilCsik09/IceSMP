name: Log fix source export

on:
  pull_request:
    branches:
      - master
    paths:
      - '.github/workflows/log-fix-source-export.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  export-source:
    if: github.head_ref == 'fix/log-errors-2026-08-02' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: Check out full repository history
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Remove persisted checkout credentials
        shell: bash
        run: |
          git config --local --unset-all http.https://github.com/.extraheader || true
          git remote set-url origin https://github.com/MilCsik09/IceSMP.git

      - name: Create complete Git bundle
        shell: bash
        run: |
          git bundle create /tmp/icesmp-log-fix.bundle --all
          git bundle verify /tmp/icesmp-log-fix.bundle

      - name: Upload complete Git bundle
        uses: actions/upload-artifact@v4
        with:
          name: icesmp-log-fix-git-${{ github.sha }}
          path: /tmp/icesmp-log-fix.bundle
          compression-level: 0
          retention-days: 1
          if-no-files-found: error
