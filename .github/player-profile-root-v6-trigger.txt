Trigger isolated PPROOT2 validation for the PlayerProfile root platform.
Attempt: 12 — replace the two obsolete resetClassSpecSectionV2 command call-sites with the canonical Profile-backed resetClassSpecSection API, fast-forward the recovered v4 branch, then run Java 21 build and PlayerProfile guards.
