## Current continuation — native developer Trash mutation receipts (2026-09-09)

The full WW implementation mandate remains active. This is a native implementation step, not full provider or release completion. Source parent `6f11bf89907cc86797eb1595838d41618e995f05`, tree `65083c0c7cd0d247d8f2c8d9fcef83d015a0d06e`, remains on feature/#159/base #158. Parent WW `34398551019` and Trash `34398551111` failed at the existing exact-source vendor assertion in TrashHistoryRegressionSuite: the new prototype refusal had combined its condition with the malformed-item check. The checks are now separate and that regression passes without changing its gate. All four actual parent native Trash jobs (`102624385049`, `102624385166`, `102624385199`, `102624385279`) passed the prototype probe and shutdown; cooperative probes explicitly have zero connected players. Parent Docs `34398551089` and resources `34398551098` pass. Parent runtime success is not successor execution evidence.

TrashHistoryService now prepares and commits four native developer mutations: individualization, authored success transition, repair and sandbox copy. Plans retain detached complete owner-inventory before state, exact source/destination slots, actual native target UUID, five-second deadline and one-use final admission. Native inspection/history and exact owner inventory must still match before the permit is spent. A copy leaves its source byte-identical, receives a fresh UUID and DEV_PROTOTYPED provenance, and copies no natural history or owner milestones. Other developer edits preserve actual natural origin while using explicit DEV_INDIVIDUALIZED/DEV_TRANSITIONED/DEV_REPAIRED events. Archaeology does not interpret those events as natural repair/transformation or historical evidence. Native Trash prototype origin also keeps quarantine detectable if custody markers are removed.

The existing TrashHistoryStore and its same WAL now atomically retain the native history change and exact one- or two-slot byte projections. Snapshot schema 6 accepts legacy 3–5; WAL schema 4 accepts legacy 1–3. Developer receipts are separate native operation records, not vendor or projectile receipts and not a new store. Each receipt is bounded to 65536 encoded characters, all retained receipts to 16 MiB/1024 operations, with refusal instead of eviction. A derived pending-instance index fences normal history use until exact owner projection is observed. Observation is a separate WAL acknowledgement; it does not establish Minecraft playerdata durability. Completed receipts are retained and never authorize recreating missing items. Pending before-state reconciliation refuses conflicting slots and duplicate output UUIDs elsewhere in the inventory. Lost acknowledgements fence unassessed memory until real disk reload. Developer events cannot commit without their corresponding native receipt in the same transaction.

Local verification: all 1448 Java main/regression sources compile with zero errors and three inherited warnings. Real storage suites pass: developer receipts82, wall receipts124, wall handoff575, shared rule creation452, native inspection97; existing Trash history, archaeology and production-hardening regressions pass. Tests cover fsynced lost acknowledgement, rollback, held/reentrant writers, exact observation, retained completion after history compaction, schema corruption and the 1024-operation cap. The native Paper/Folia probe now exercises all four mutations on actual detached ItemStacks, exact slot recovery and prototype provenance. It requires successor CI; it is not connected owner/playerdata evidence. Profile classification adds reviewed existing-WAL and derived-index categories, never a raw PlayerProfile bypass.

Next: bind the native operations into authenticated WorldWeaver actions, exact item-subject recovery and conditional Undo, and finish the shared native anomaly/relic activation and field actions. The four native APIs are not yet registered WorldWeaver actions or connected-player issuance. Complete remaining WW04 and WW06–WW09, all 51 whole-domain blockers and real connected/runtime/client/builder evidence. Missing authored NPC bindings stay NONFATAL; required FancyNpcs initialization remains separate. Same draft stack and publication authorization; no merge, release or production enable. Public documentation changes only the verified trash package count (43 to 44), mirrored byte-for-byte.

## Previous continuation — intrinsic prototype identity and native quarantine (2026-09-09)

The full implementation mandate remains active; this is not a task-completion or provider-closure claim. Source parent is `480c5146ba55b1f6a7f6446cc6a195250d7346dc`, tree `b5b4caf39d9c2e62f1b926de0dcc030cc7289c67`, on the same feature/#159/base #158. All four parent workflows succeed: WW `34394645568`, Trash `34394645526`, resources `34394645404`, Docs `34394645560`. Decoded build job `102611209656` confirms full build in 1m51s plus a 13s evidence invocation, including creation452/provider298/causal56. Actual native Trash jobs `102611209693`, `102611210047`, `102611210052`, `102611210149` pass; cooperative probes have zero connected players. Docs job `102611209221` confirms 50 tests/655.732s, zero FAIL/WARN consistency and zero reported blocking inventory findings. Parent evidence does not prove this successor.

Native `ItemMutationService.clonePrototype` produces a detached candidate with a fresh item UUID, explicit `DEV_PROTOTYPE` state, original-source reference in a separate `DEV_PROTOTYPED` history event, and exact developer/operation provenance. It copies actual authored roll/rune/ascension state but does not copy natural crafting/masterwork/history claims. This pure builder grants no issuance authority and is not yet a WorldWeaver item mutation action. Native codecs, reroll, ascension and rune transitions preserve intrinsic prototype identity. `ItemIdentityService` regenerates and verifies `dev_prototype` custody metadata from that identity and keeps attributes, enchants, ability power, rune/set/signature contribution suppressed through render/reconciliation. Removing marker PDC cannot turn an intrinsic prototype into a normal managed item. The existing `dev_item_id` artifact remains distinct.

Shared `ItemPrototypePolicy` recognizes direct and bounded nested bundle/container/charged-crossbow custody, including malformed markers. An unreadable nesting limit refuses a transformation without deleting an unidentified ordinary item. Only a singleton matching the immutable HiddenDevAuthority primary developer (not a configurable artifact owner or OP) and owner metadata remains in personal inventory/cursor. Native DEV owner refresh, join/respawn, hand/inventory activity, ender chest, container views, entity reload and bounded 32-container chunk callbacks perform owner-local quarantine. Physical movement, drop/pickup/hopper, bundle/creative, use/place/consume/launch/attack/harvest and craft/smith/crafter boundaries are guarded. The existing native transformation policy denies every prototype transformation route. Market, Trash vendor, salvage, normal item mutations/material payment, equipment authority, signature/rune consumers, native Trash behavior and archaeology progression separately refuse prototypes. Hidden Trash inspection includes the native prototype marker and valid custody identity.

Final local verification: all 1446 main/regression Java 21 sources compile against 49 actual CI dependency jars with zero errors and three inherited warnings. Itemization140, vanilla-boundary125, equipment284, rune38, creation452, handoff575, receipts124, native-inspection97 and provider298 assertions pass, plus relic/production hardening. Profile guard: 703 classified, zero unknown/stale/invalid/transition; four architecture tests; consistency/mirror zero FAIL/WARN. IceSMPGuides draft #20 is updated at `d18a150f3fd4a0a00fedc2db8a4a7b4cb61db0a9`. The successor native Paper/Folia probe tests actual detached ItemStacks, serialization, nested containers, marker loss, canonical render suppression and real transformation classification; only its eventual current-source CI logs constitute execution evidence. No connected-player custody, Minecraft playerdata crash recovery or prototype issuance is claimed.

Coverage now classifies 341 authorities / 55 domains / 51 whole-domain implementation blockers. The new native policy belongs to itemization; its physical adapter is assigned to itemization/Trash/developer-self without changing any full-provider status. The only public documentation change is the verified listeners package count (123 to 124), mirrored byte-for-byte. Listener wiring is below command registrations, so registration locations are unchanged; only reviewed core/probe source digests change. The previously recorded strict inventory component findings remain unresolved and are distinct from report-mode CI success.

Continue all seven native Trash developer actions and their existing-WAL receipts/conditional recovery, complete shared Anomaly/Relic activation and the WorldWeaver item mutation ingress using the existing `ItemMutationCoordinator`/journal/render path. Do not issue a prototype through the detached builder directly or add a second inventory/history ledger. Remaining WW04 ingress and full WW06–WW09 scope stay required. Missing authored NPC bindings remain NONFATAL, required FancyNpcs plugin/API startup separate. Feature/draft stack publication is authorized; no merge, release or production enable.

## Previous continuation — shared native rule creation and consequence lineage (2026-09-09)

The owner now explicitly requires completion of the full WW implementation without stopping at individual checkpoints. This checkpoint is ongoing implementation, not closure. Source parent: `185bcacea08be7347fb42cedbdbe517f7c856ef6`, tree `3837a391434560e0b23bd7f4a3ae38cd999a9e31`; existing feature/#159 and base #158 `ac6e46eb96a6d4cd8b00e778afb775acdeb2ae4c` remain unchanged. Resolve the public successor HEAD before resuming. All four parent workflows pass: WorldWeaver `34389427664`, Trash `34389428348`, resource pack `34389427478`, Docs `34389427651`. Decoded parent build job `102593791484`, native Trash jobs `102593794759`, `102593794823`, `102593794886`, `102593794948`, and Docs job `102593791888` confirm their actual evidence. Cooperative probes have zero connected players; parent evidence is not successor evidence.

All four native rule kinds now share `TrashRelicActivationService` inactive reservation, owner dispatch, two independent journal permits, final native history admission and acknowledged field publication. The three success-transforming fields use the real selected hand, planned native singleton UUID, authored phase and original natural origin in the existing history WAL. An exact `CreationWrite` capability retains existing capacity while an entered acknowledgement outlives deadline/owner cancellation. No replacement field/history authority, executor, synthetic event or refund is added. Named creation ports retain UUID/serialized item data/scheduler handles and resolve current ownership before live reads.

Combat and acoustic event consequences use native field identities and actual affected entity/location sources. Pending provenance writes never retain or replay a Bukkit event: only an immediately acknowledged permit may modify that event; a later native event captures afresh. Trash contributes each actual active overlapping field Event through generic causal-source discovery. Preparations, expired fields, other worlds and geometry misses are excluded. Two existing source-gated PvE knockbacks explicitly use the native spatial-anchor policy. This does not claim complete remaining Relic/Anomaly influence ingress.

Source budgets remain explicit and frozen at registry publication: existing providers default to 16; Trash advertises its actual native 32/world bound; the combined extension budget is 48, leaving room for native causes within the unchanged 64-source context. Nothing is silently truncated. Tests cover 32 actual overlapping fields, invalid/default/frozen provider budgets and exact combined-budget acceptance/refusal. The reviewed core composition addition shifts 68 registration evidence locations by one line; exact command/permission identities, aliases, counts and all gates remain unchanged.

Local evidence: all 1444 main/regression Java 21 sources compile with zero errors and three inherited warnings against 49 actual CI dependencies. Native creation/consequence suite 452, owner handoff 575, receipts 124, native inspection 97, fields 294, provider inspection 298, generic causal capture 56 and influence propagation 227 assertions pass, plus relic and production hardening. The new native runtime probe verifies planned success transitions with real detached ItemStacks/history WAL; its actual Paper/Folia execution still requires successor CI logs. Local Gradle 9.4.1 offline build cannot resolve the uncached run-paper 3.0.2 plugin; it is not a full-build pass. Consistency/public mirror: zero FAIL/WARN; four architecture tests; coverage 340 authorities / 55 domains / 51 whole-domain blockers / zero audit errors. Strict documentation inventory's 249 component findings remain open, distinct from report-mode CI success.

Continue the complete native activation APIs, all seven Trash developer actions, prototype quarantine and native durable mutation/recovery, then remaining WW04 and WW06–WW09 requirements. Connected owner regions/playerdata custody, actual builder/world/NPC data and multiplayer/client/human acceptance remain unproven. Missing authored NPC bindings remain NONFATAL; required FancyNpcs plugin/API startup is separate. Authorized: feature commit/push and existing draft stack. No merge, release or production enable.

## Previous continuation — native wall interception influence admission (2026-09-09)

This is the current handoff and supersedes older creation-only and ungated-interception statements below. Source parent: `ab4efb2d87e2afde08007eb585731c8cf62d48a3`, tree `cf3f20e3d54ab426186e4922119b610bd8fa91c8`; same feature/#159, unchanged base #158 `ac6e46eb96a6d4cd8b00e778afb775acdeb2ae4c`. Resolve the live successor HEAD before resuming. All four parent workflows succeed: WorldWeaver `34360383770`, Trash `34360383491`, resource pack `34360383806`, Docs `34360383496`. Decoded parent build job `102495539657` confirms full build success; this evidence belongs to the parent. The primary resume, normative design and coverage authority order remain unchanged.

Both co-owned and cross-owner projectile-wall interception now use the existing `TrashRelicActivationService`; the old direct consuming/removal fallback is removed. On the inventory owner, the runtime prepares two distinct single-use `GameplayEffectPermit`s through the existing journal, using actual field Event, native item UUID, player, world/location and projectile causal sources. The actual item and projectile receive durable original lineage before native consumption. The native history writer claims its consumption permit at final admission after repeatable freshness/compaction checks. Only an acknowledged native consume+receipt may schedule final projectile admission, which rechecks fresh owner-local projectile/field sources before actual observed removal. Field/item/source drift or policy-binding retirement refuses the next consequence. No second field/history authority, synthetic event, effect replay, fabricated completion or refund is introduced.

Preparation holds the existing exact field claim and tracking ticket, with a cancellable five-second deadline. If deadline/retirement arrives during an entered native WAL acknowledgement, its claim/capacity stays held until that acknowledgement returns. After an acknowledged consumption but refused removal, the real pending native receipt survives restart. Deadline registration races cancel exactly once; synchronous/asynchronous preparation failures report through existing aggregate telemetry and release their claim without gameplay mutation. Named owner ports and the launch-tracking callback retain only detached data and scheduler handles; UUID lookup and ownership checks precede live entity reads. Wall creation now similarly carries serialized before-item data and UUID across journal callbacks and re-resolves the owner before native admission.

Final local source evidence: all 1443 main/regression Java 21 sources compile against the 49 real cached CI dependencies, zero errors/three inherited warnings. Owner handoff **575**, retained receipts **124**, native inspection **97**, fields **294**, provider inspection **257**, influence propagation **227** assertions pass, plus native history/relic/production hardening suites. New tests exercise actual native WAL/fsync and WeaverJournal lineage/restart with deterministic owner queues, including deadline during held acknowledgement, delayed preparation, stale owner sources, new field influence before/after consumption, fresh projectile influence and retired policy binding. Source guards follow the common native path and retain positive-observation/aggregate-error checks. These are not connected Folia regions, live inventory/playerdata durability or populated interception evidence. Native history WAL remains synchronous; this checkpoint does not claim a fully nonblocking general activation API. The local Gradle distribution download is unavailable; the exact successor still requires its own full GitHub build and runtime smoke logs.

Coverage is 340 authorities / 55 domains / **51 whole-domain implementation blockers** / zero audit errors; its stage now correctly names WW05. Only reviewed native source digests change. Profile authority guard: 698 classified, zero unknown/stale/invalid/transition findings; four architecture tests and consistency/mirror checks pass. Public documentation is unchanged. The 249 strict inventory component findings recorded below remain open; report-mode Docs success is not strict release acceptance.

**First next implementation work:** extend native influence/prototype admission through the other rule-field kinds and remaining Relic/Anomaly consequences, using the existing journal and canonical authorities. Complete the general shared Relic API, `TrashAnomalyActivationService`, all seven developer Trash actions and durable adapter/Undo/recovery/prototype contracts, then universal WW04 ingress and full WW06–WW09. This wall-only continuation closes no whole-domain blocker. Connected cross-region Folia, physical custody/save/recovery, builder/world/NPC content, multiplayer/HUD and human acceptance remain evidence-required. Missing authored NPC bindings stay NONFATAL by explicit owner instruction; required FancyNpcs plugin/API initialization is separate. Commit/push on this feature is authorized; no merge, release or production enable.

## Previous continuation — native wall creation influence admission (2026-09-09)

This is the current continuation and supersedes older statements that all native Trash influence wiring is absent. The primary handoff remains this document; WW-05-territory-policy.md is supporting evidence, coverage.json is the inventory, and design-v2.md remains normative. Resolve current public HEAD first. Source parent is `9932473940c6e228d0faf4f2567e7737f2a35cf8`, tree `7e03a1003fde98101d57a56001cc06e2ee8a9e65`, same feature/#159/base #158 `ac6e46eb96a6d4cd8b00e778afb775acdeb2ae4c`. Do not reset to the parent. All four parent workflows succeed: WorldWeaver `34358087268`, Trash `34358086943`, resource pack `34358086999`, Docs `34358087036`. Actual build job `102487775521` and all four Trash jobs `102487763901`, `102487763913`, `102487763975`, `102487764042` were decoded: full build and Paper/Folia direct/cooperative reload/startup/shutdown pass. The planned-unit detached ItemStack probe executed there. Cooperative cases have zero connected players; no connected wall interception or durable Minecraft playerdata claim follows.

Decoded parent Docs job `102487763428` passes 50 tooling tests and consistency 0/0, but reports 70 inventory FAIL findings in report mode. These are now corrected at their cause: the owner-requested NPC callback change shifted 68 actual registrations by exactly six lines and changed both reviewed core-source digests. Names, implementation classes, aliases, routes, permissions and counts are unchanged. The exact manifest retains all gates and updates those source locations/digests plus its entry digest. A stricter local run confirms these 70 command/permission findings are gone, but fails on 249 separate UNDOCUMENTED_INVENTORY_ITEM component entries across the cumulative source. These remain genuine strict-release inventory work; report-mode success must not be represented as strict acceptance. Do not describe the parent's green Docs workflow as a zero-finding inventory result. The old `28c21e9` WorldWeaver failure was the obsolete NPC-shutdown assertion; `9932473` already repairs it while preserving actual shutdown cleanup/authority tests.

Native projectile-wall creation now captures real owner-local item/player/world/location sources and uses the actual native UnitPlan UUID plus the actual `trash.rule_field` Event UUID as influence targets before history or physical mutation. Both targets use the existing monotonic journal lineage. A bounded preparation is reserved in the existing TrashRuleFieldService before asynchronous journal admission, shares the existing 32/world and 128/global caps, and remains invisible to effect consumers. No parallel ledger, native identity authority, synthetic event, executor or developer action registry is introduced. The provider exposes pending preparations read-only, world-filtered, without a reservation token or a fake active-field claim.

The actual owner callback rechecks lifecycle, ownership, online/world identity, exact hand/slot/full source stack and unexpired preparation. Native history claims a single-use permit immediately before its WAL transaction and stamps the real reservation token inside the existing inventory projection/rollback boundary. Only after native WAL acknowledgement does a second single-use permit recheck freshly captured influence sources and activate the exact preparation. New influence, expiry, source drift, retirement, rejection or shutdown cannot activate a stale field. After acknowledged history but rejected field publication, no active effect is created; the reservation is cleared without inventing a refund or rolling back acknowledged history. Pending preparations are cancelled on owner cleanup, world unload, expiry and close; old/foreign callbacks cannot release replacements or active fields.

Local source verification: all 1443 main/regression Java sources compile with zero errors and three inherited warnings. Native field suite 294, influence propagation 226, provider inspection 257, native inspection 97, retained receipts 124 and owner handoff 204 assertions pass. Actual journal/storage fixtures verify durable original lineage on item/field targets before publication, retained lineage after removal, refusal of post-WAL source changes and single-use admission; field fixtures cover immutable snapshots, inactive preparations, mixed/concurrent/world/global capacity, stale/foreign identities and lifecycle cleanup. These are source/native-storage fixtures, not connected Folia players. Profile guard is 699 classified findings/zero unknown or transition, coverage 340 authorities/55 domains/51 whole-domain implementation blockers/zero errors; architecture four tests pass. Successor full CI must be resolved separately from the parent.

**First next task:** carry native field/item/player/world/projectile influence through actual cross-owner wall interception and both acknowledged consumption and final projectile consequences, with fresh owner admission at each effect boundary. Current wiring covers projectile-wall CREATION only; other field kinds and the remaining native relic/anomaly behavior paths are still not closed. Extend the existing TrashRelicActivationService/history/field and WorldWeaver journal seams; do not claim complete Trash influence or prototype custody. General Relic activation, TrashAnomalyActivationService, all seven developer Trash actions, prototype custody, universal WW04 ingress and WW06–WW09 remain unfinished. All 51 whole-domain blockers remain. Real cross-region Folia, connected custody/playerdata, builder/world/NPC content, multiplayer/HUD and human acceptance are evidence-required. Missing NPC bindings remain NONFATAL by explicit owner instruction; required FancyNpcs plugin/API initialization remains distinct. Commit/push on this feature is authorized; no merge/release/production enable.

## Native planned-unit admission and current CI correction — 2026-09-09

Current source parent is `28c21e9d3ecdbf51d14143fbeb25eb08ce9cd80a`, tree `4069f5d7ebbea80c2c02cb84a98fd0d89688c77f`, same feature/#159/base #158. The parent's WW workflow `34356041309` failed only at resourcePackRegressionTest: decoded job `102480843168` reports the obsolete assertion "NPC readiness gate was bypassed". ResourcePackDeliveryRegressionSuite still required shutdown for an unhealthy authored NPC snapshot despite the owner's explicit nonfatal-binding decision. The assertion now requires retained diagnostics and absence of NPC-driven disable; real cooperative owner-cleanup/profile-teardown assertions and the separate required FancyNpcs plugin/API contract remain. ResourcePackRegressionSuite passes (delivery 31); the NPC binding decision is not reverted. Parent Trash `34356041216` and resource `34356041270` succeeded; resolve Docs `34356041215` and successor CI separately.

The existing TrashHistoryService now prepares an opaque UnitPlan with the actual future native UUID before physical/history mutation. Preparation validates the actual singleton, canonical content, origin and current history, refuses pending walls or unavailable native state, and stores only a detached before-item plus authority-bound single-use state. There is no pending-plan registry, ledger or persisted second identity. Plans expire after five seconds; the commit rechecks exact item and native history under the existing writer lock. Fresh targets must remain absent; tracked targets retain their real UUID/revision. A different service cannot consume the plan. The native hand-individualization path uses the same plan and preserves the actual slot/hand, full inventory remainder and rollback contract.

TrashHistoryStore has a final-admission overload: repeatable freshness is checked before and after actual compaction; a single-use final permit is claimed exactly once immediately before native mutation. Busy/reentrant/initial or second freshness rejection does not spend the permit. Final refusal writes no event and a consumed plan cannot retry after refusal, rollback or lost acknowledgement. Existing native WAL, origin-event ordering, history capacity, pending-wall fences and uncertainty semantics remain authoritative; no schema migration is added. The generic neutral GameplayEffectPermit is exercised by regressions; full GameplayEffectGate/journal wiring is not yet implemented by this preparatory seam.

Fresh local evidence: all 1443 main/regression Java sources compile with zero errors/three inherited warnings. Native inspection suite now has 97 assertions plus preserved receipt124/handoff204 suites, using actual native WAL/fsync, held writers, real 1024-record compaction, single-use permit rejection/replay and lost acknowledgement. Existing history/relic/production hardening, resource-pack and NPC regressions pass. The real Paper/Folia native probe adds detached ItemStack tests for side-effect-free target preparation, source/authority/history drift, final refusal/no retry, exact origin/UUID, rollback and WAL reload. Those new ItemStack assertions require successor runtime CI; they are not connected inventory/playerdata evidence.

**First next implementation task:** bind these actual planned target IDs into GameplayEffectGate/WeaverJournal before native wall/item creation. Capture immutable native item/player/world/location/field causal inputs on their owners, use the single-use final permit at the new native boundary, and bound pending activation in the existing native activation/field authority. Complete field and interception consequence propagation as well as prototype custody; do not claim this planning API alone propagates influence. Shared Relic activation is still wall-only; Anomaly activation and all seven developer Trash actions remain absent. WW04 universal ingress and full WW06–WW09/51 whole-domain blockers remain. Keep developer mutations and production gates disabled until their documented contracts close. Missing NPC bindings remain diagnostic only by explicit owner instruction.

## Explicit owner decision — missing NPC bindings are nonfatal (2026-09-09)

The user explicitly requires that missing/mismatched NPC bindings must not disable IceSMP. This supersedes earlier handoff statements requiring whole-plugin shutdown for an incomplete authored NPC snapshot. The required FancyNpcs plugin/API initialization contract remains unchanged; authored content completeness is now diagnostic only. The delayed 60-second core callback still runs exact-name/reference validation, but has no disable action. Diagnostics explain that IceSMP stays active and affected NPC paths remain unavailable. Valid NPC interactions continue through the existing QuestManager source/progression authority; validation grants no completion/reward and creates no NPC coordinates. Public admin/builder/ROADMAP documentation and WORLD-03A acceptance reflect this requested behavior.

Local verification on the current successor: all 1443 Java main/regression sources compile, zero errors/three inherited warnings; QuestNpcValidation passes with the nonfatal-binding guard, GameplayBootstrapIntegrity 38 and CommandLifecycle 29 assertions pass; consistency 0 FAIL/0 WARN, architecture four tests, coverage 340/55/51/0, Profile 698/zero transitions. Full successor GitHub CI and WORLD-03A connected acceptance must be checked separately.

Source parent: a28bf77bad158dcab1d1f1cc3eefd262b201d898 (all four workflows succeeded). Resolve the current successor HEAD and its own CI before continuing. No WW influence/prototype implementation is included in this bounded owner-requested change. The full 51-domain-blocker scope, native Trash influence/prototype first task, runtime/client/builder evidence gates and no-merge/no-production-enable restrictions below remain binding. Missing NPCs remain a content/playtest blocker, not a reason to stop unrelated plugin systems.

# IceSMP WorldWeaver — primary session resume, 2026-09-08 (updated)

## Current continuation — native cross-owner wall handoff

This section supersedes older co-owned-only and absent-Relic-service statements below. Public parent is `9e910fd4486c84271c232de9f0097f47984c333f`, tree `0e58d2e731e72e8ca50e91f87d56937e26bbc398`, same feature/#159/base #158. All four exact-parent workflows succeed: WorldWeaver `34290555483`, Trash `34290555530`, resource pack `34290555494`, Docs `34290555497`. Actual decoded build job `102275912660` confirms receipt 124/native inspection 81/fields 235/provider 248 and BUILD SUCCESSFUL. Actual four Trash logs `102275912780`, `102275912816`, `102275912829`, `102275912878` confirm Paper/Folia direct/cooperative reload, startup and shutdown. The observed-retention detached-ItemStack fixture executed there; cooperative cases explicitly have zero connected players. Decoded Docs job `102275912481` confirms 44 tests, consistency 0/0 and zero report-mode blocking inventory findings. Resolve the successor's public SHA/CI before continuing; never reset to this parent.

The design-named native `TrashRelicActivationService` now coordinates the projectile-wall cross-owner path. This is a partial native Relic activation extraction, not the complete general activation API. It shares the existing TrashRuleFieldService, bounded projectile tracking and native history receipt; it has no independent registry, ledger, executor or mutation authority. Dispatch transfers the exact field claim/tracking ticket to the inventory owner, records consume+pending receipt through the existing native transaction, then schedules the projectile owner. The final projectile callback rechecks lifecycle, exact claim, validity, world and current containment before removal. No projectile freeze, foreign inventory read, synthetic Bukkit event or effect-before-WAL path is introduced. The co-owned fast path is retained.

Native final effect admission and observation are serialized with claim release/close in TrashRuleFieldService. This bounded owner-local section contains no WAL or scheduler wait; acknowledgement happens afterward. Duplicate/late callbacks cannot consume or remove twice. Refusal before consumption releases the native claim/ticket without changing history. Expiry, owner retirement, scheduler failure or shutdown after acknowledgement retains the pending receipt and reports the unresolved effect. Removal-triggered scheduler retirement is fenced so an entered observation/finally boundary is not falsely reported as failure. Exact token cleanup cannot release a replacement claim; lost acknowledgements remain fenced until real WAL assessment. Missing/old physical custody evidence is not converted into a refund or success claim.

Local source evidence: all 1443 Java main/regression sources compile with zero errors and three inherited warnings. Native owner-handoff suite passes 204 assertions; receipt suite 124, original native inspection 81, native fields 243 and provider 248 pass, with affected relic/hardening suites. Tests use deterministic owner queues over actual native fields, tracking and fsynced history/WAL, including duplicate callbacks, stale claims, busy writers, scheduler rejection/exception, inventory/projectile retirement, position refusal, expiry, false removal observations, real lost WAL acknowledgement and shutdown while acknowledgement is held. Final field-observation tests serialize actual concurrent close versus an entered effect. These are not real Folia regions or connected playerdata/projectile evidence. The successor still needs its own full GitHub build and existing smoke runs; those smoke probes have no connected cross-owner interception. Config/content audit (24634 leaves, 160 armor, 89/61 PvE, 69 commands, zero generated gameplay), resource helper 18 cases, architecture four cases, consistency 0/0 and Profile 698/zero transitions pass. Coverage is now 340 authorities, 55 domains, 51 whole-domain implementation blockers and zero audit errors.

The duplicate-method consistency scanner now uses actual named/anonymous type-body scopes rather than the nearest preceding type declaration. Six focused tests preserve true duplicate detection (including outer methods after nested types) while accepting independent native owner adapters. The existing Java scanner is extended; no gate or authority is bypassed. The public architecture inventory is 43 Trash classes and records the new native coordination component under the existing history feature; the IceSMPGuides architecture file is mirrored byte-identically in published `c782fc2e953e77b1de0b1f726d59cacf5cf05e29` (tree `01fd4f5bd55f05beacd3da6e288492dd3ccb1ae4`), same feature and draft #20.

**First next implementation work:** native Trash influence/prototype admission and propagation across actual activation, rule fields, item consequences and rewards, extending existing WorldWeaver influence/receipt seams. Review all retained uncertainty paths and physical-before marker limits; do not infer Minecraft durable saves or replay/refund missing outcomes. Then complete the shared Relic activation API beyond wall coordination, extract TrashAnomalyActivationService, implement all seven developer Trash actions and their journal/Undo/recovery/prototype contracts, and finish WW04 universal ingress plus full WW06–WW09. Developer mutation/wall actions remain unregistered; full Trash coverage and the 51 implementation blockers are not closed by this native handoff. Full Folia cross-owner runtime, connected physical custody/save, builder/NPC, multiplayer/HUD and human acceptance remain evidence-required. No merge, release or production-enable verdict is authorized.


## Current continuation — retained observed wall recovery and bounded acknowledgement

This section supersedes current-state statements in all chronological checkpoints below. Public parent is `204cbfaade47d39ea2cf8833f0a7114ca251d5b9`, tree `8facc7cbf3a4d1bcd92541e45cd687d84fbc69f7`, on `feature/world-weaver-ww05-trash-territory`, open draft #159 based on unchanged #158. Resolve this successor's actual remote HEAD before resuming; never reset to a recorded parent. The complete WW00–WW09 user request is still active, with commit/push authorized, no merge/release/production enable.

All four exact-parent workflows succeed: WorldWeaver `34287911330`, Trash `34287911298`, resource pack `34287911282`, Docs `34287911290`. Decoded build job `102267647104` confirms BUILD SUCCESSFUL, receipt 82 / native inspection 81 / fields 235 / provider 248. Decoded actual Trash logs `102267647286`, `102267647447`, `102267647451`, `102267647472` confirm Paper/Folia direct/cooperative reload, startup and shutdown; cooperative cases have zero connected players. These execute the detached stale-item projection fixture, not connected playerdata durability or projectile interception. Decoded Docs job `102267647551` confirms 44 tests, consistency 0 FAIL/0 WARN and zero report-mode blocking findings. The seven stacked parent PRs were rechecked unchanged; the parent table later in this document remains valid for #152–#158. IceSMPGuides #20 remains at `27284ee93864c3f1429f79dcbf22d4ef0163a165`, with all 14 public documents byte-identical.

The completed-operation retention gap identified at `204cbfa` is now implemented in source. Native history snapshot schema 5 reads 3/4/5; WAL payload schema 3 reads 1/2/3. Positive removal acknowledgement changes the exact receipt to immutable `removalObserved=true` instead of deleting it. Observed receipts remain within the existing 100000-instance native history lifetime; unresolved capacity remains 1024 with no eviction. A derived instance-to-operation index and unresolved ID set are rebuilt from the same canonical receipts under the same transaction/load/rollback lock. They are not a second authority. Legacy schema-2 explicit completion records retain their existing receipt as observed; an already compacted legacy snapshot with no receipt cannot be reconstructed from absence.

A bounded owner-captured query (at most 64 inventory instance IDs) exposes both pending and retained observed receipts to the existing recovery path. Normal pending inspection still lists only unresolved effects. Pending receipts continue to fence history mutation. Observed receipts permit later native history events and survive bounded event pruning; automatic old-item projection refuses if canonical history has advanced beyond the consumed revision. Exact actor, instance, phase, before revision, reservation token and unchanged locally unique owner slot remain required. Missing units, missing reservation evidence, duplicates and drift require review; nothing is created, refunded or replayed. A successful live ItemStack projection does not retire the native receipt and is not treated as Minecraft save proof.

Observed-removal acknowledgement now recognizes an exact already-durable receipt without another write or observer call. The live native path starts a bounded retry only after a positive owner-local `remove` observation. Up to 20 delayed attempts at 250 ms use the existing plugin AsyncScheduler with immutable receipt data; there is no entity/inventory access, second ledger, executor or effect replay in the callback. Closed runtime/plugin stops retries, scheduler rejection or exhausted retries preserves the pending receipt and aggregate telemetry. A process crash before observation reaches the native WAL remains genuinely unassessed.

Local evidence for this successor: full 1441-source Java 21 compilation, zero errors/three inherited warnings; native wall receipt/recovery regression 124 assertions, original native inspection 81, fields 235, provider 248 and all native Trash suites including 30M distribution pass. Schema/type/transition corruption, pre-observed insertion, observation rewrite/retirement, legacy migration, lost acknowledgement after real fsync, busy/reentrant refusal, retained projection, capacity and event-pruning cases use actual native files/WAL. Existing Paper/Folia probe is extended with detached observed-receipt recovery and exact idempotent acknowledgement; it needs successor CI before runtime PASS. A positive observation in that isolated storage fixture is explicitly fixture input, not a spawned projectile. Consistency 0/0, architecture four tests, Profile 698 classifications/zero transitions and coverage 339 authorities/55 domains/51 implementation blockers/zero audit errors pass. Exact successor CI remains required after publication.

**First next implementation work:** finish the native cross-region owner handoff and its lifecycle/retirement/expiry/recovery contracts using these exact receipts and claim identities. The current runtime still restricts interception to co-owned player/projectile entities; no projectile freeze or foreign inventory access may return. Native influence admission and genuine crash outcome assessment remain open. Do not register a developer wall action before that closure. Then extract canonical shared anomaly/relic activation APIs, implement all seven developer Trash actions and their durable adapter/Undo/prototype/influence integration, and finish WW04 universal ingress and full WW06–WW09. All 51 whole-domain implementation blockers remain; no full-wall/full-provider, merge-ready or production-ready claim is made.

Remaining external gates include the actual 18 authored NPC/world bindings, the user's deployed JAR/hash diagnosis, connected two-region HUD/pack/command shutdown, connected physical custody/playerdata/projectile tests, builder worlds, multiplayer and human acceptance. The already published lifecycle and inventory fixes are not to be redone or treated as deferred; historical deferral sections below were superseded by the user's instruction to fix findings first. Equipment audits and all fail-closed gates remain intact.


## Native pending-consumption projection recovery

Public parent for this follow-up is `8ee1dc6555ec8f790297877b652ccd44780d0e03`, tree `88d0967dafc38eacd8ac83a6f55f2831f2fdbadd`, same feature/#159/base #158. This publishes the exact claim-token repair described below. WorldWeaver `34286054024`, Trash `34286054098` and resource pack `34286054042` succeed; Docs `34286054060` also succeeds: decoded job `102261799048` confirms exact HEAD, consistency 0 FAIL/0 WARN, 44 tooling tests and zero report-mode blocking inventory findings. All four workflows on `8ee1dc6` pass. Decoded build job `102261799330` confirms field 235 / receipt 69 / native inspection 81 / provider 248 and BUILD SUCCESSFUL. Actual four Trash logs `102261799768`, `102261799995`, `102261800118`, `102261800314` confirm reload/startup/shutdown and the zero-connected-player cooperative scope. All four earlier `831a5f1` workflows also succeeded, including Docs with zero report-mode blocking findings.

This follow-up adds a conditional native projection repair for an existing singleton whose saved physical marker still matches the exact before-revision of an acknowledged **pending** wall receipt. The native history/WAL remains unchanged; the existing TrashItemFactory applies the acknowledged consumed phase and the native history service stamps the already committed revision. No new history event, world effect, refund or missing physical item is created. The callback has rollback and final owner/slot admission after compaction. A stale/completed receipt, another actor, malformed marker, wrong phase/revision, duplicate-sized stack, unavailable store or owner refusal cannot enter projection.

Native startup/join/stale-reservation cleanup invokes this repair only on the inventory owner, for the original actor, one locally unique instance slot, exact reservation token and unchanged item snapshot. The inventory is indexed once per pass; retained receipts are detached bounded data. Missing or locally duplicated instances remain unresolved. Pending or unassessed receipt state preserves reservation markers. A later brick-use attempt retries the same native assessment; this is not an automatic effect replay. A projection of an existing item is not proof that Minecraft playerdata was durably saved or that another physical copy cannot exist elsewhere.

Local tests pass: receipt suite 82, original native inspection 81, fields 235, provider 248, existing history/relic/hardening suites and all Java compilation (1441 sources, three inherited warnings). The existing real Paper/Folia probe now includes an isolated native-history and detached-ItemStack fixture for stale physical projection after WAL reload, missing/wrong-actor/duplicate/owner refusal, projection rollback, canonical after-state, repeat refusal and unchanged WAL bytes. This additional probe is **not yet runtime evidence** until this successor's actual CI logs pass. It models a stale physical snapshot, not a connected player or a real projectile interception.

**First remaining recovery blocker, explicitly identified by source review:** successful `tryConfirmWallRemoval` currently retires the pending receipt before Minecraft inventory-save acknowledgement. A crash after observed removal/receipt retirement can leave old physical playerdata with no remaining wall receipt. The new repair deliberately covers only still-pending receipts; it does not solve this completed-operation retention window. Extend the existing native operation state with bounded observed-completion retention and safe retirement/recovery semantics; do not infer a durable Minecraft save from an in-memory ItemStack or replay effects/refund on missing evidence. Preserve compatibility fencing, exact operation/claim identities and native mutation fences. Busy observed-removal confirmation, full cross-region execution and native influence remain open. No completed-operation retention protocol has been implemented in this checkpoint.

Then continue shared activation and all seven developer actions, WW04 universal ingress and full WW06–WW09 as required below. This narrows one recovery gap; it does not close the 51 full-domain blockers or justify enabling developer wall creation. All external builder/client/playerdata/multiplayer/human evidence gates remain required.

## Latest native claim continuation

The pending-receipt source below is published as `831a5f1edafc7835004701fec4625ec06bc2626e`, tree `dcbbfa51c3399d191381b3573e0debfe207399ca`. Its WorldWeaver `34284616348`, Trash `34284616414` and resource-pack `34284616433` runs succeed. Actual build and all four native Trash runtime logs were decoded; receipt 69 / native inspection 81 / provider 248 assertions pass and direct/cooperative reload/shutdown markers pass, with zero connected players in cooperative cases. Docs `34284616432` also succeeds. Decoded job `102257189349` confirms exact HEAD, consistency 0 FAIL/0 WARN, 44 tooling tests and zero report-mode blocking findings (69 roots, 303 routes, 79 root aliases, 99 routing aliases, 140 features). All four workflows on this source succeed.

This next source continuation replaces UUID-only claims with identity tokens in the existing TrashRuleFieldService. Runtime admission/release/consumed-field cleanup now require the exact admitted token. Delayed callbacks, a token from another service, and an old claim after remove/re-add cannot affect a new claim. Snapshot field/claim IDs remain compatible. Native field tests now pass 235 assertions; all Java sources and affected relic/hardening/provider suites pass. PlayerProfile guard is 697 classified with zero transitions; coverage remains 339/55/51. No cross-region behavior or developer wall action is enabled by this repair.

First next work remains native observable pending-operation recovery, owner-safe cross-region execution and final native influence admission, using the published receipt and exact-claim seams. Do not recreate UUID-only release, a frozen foreign handle path or a second field/history authority. Remaining complete WW scope and evidence gates below remain binding.

## Latest continuation state — native pending wall receipts

This section supersedes stale current-state and CI-pending statements in the chronological checkpoints below. The user resumed the lifecycle findings and still requires the entire WW implementation after their repair. No merge, release or production enable is authorized.

Public source before this continuation: `34549e7424847c001dacbdd715b3924bbcee20f0`, tree `0af6b2132a294828136abdf3eb9b158825cce622`, branch `feature/world-weaver-ww05-trash-territory`, draft PR #159, unchanged base #158 `ac6e46eb96a6d4cd8b00e778afb775acdeb2ae4c`. Resolve the actual successor HEAD on GitHub before resuming; never reset to this parent.

Lifecycle repair `ba830e7d729823b48e1f5b1df7b5f9eda72538c1` is published. Its full build found an exact inventory-scanner defect, fixed in `34549e7`: typed registration declarations and unchanged JavaPlugin forwarding are not new command roots; nested guard classes are not top-level commands. Real registrations and permission contracts remain scanned. The reviewed command/permission source hashes and registration lines were updated without changing root identity, aliases or permissions. Exact native config/content audit passes (24,634 leaves, 160 armor entries, 89/61 PvE rosters, 69 command roots, zero generated gameplay overlays). Docs inventory now reports **zero blocking findings**, distinct from the unchanged **51 WW implementation blockers**.

All four workflows on exact `34549e7` succeed: WorldWeaver `34281386507`, Trash `34281386427`, resource-pack validate-only `34281386457`, Docs `34281386425`. Decoded full build job `102246707439` runs lifecycle 29 / Profile teardown 37 / delivery 31 assertions and completes BUILD SUCCESSFUL. Decoded Docs job `102246706612` confirms the tested HEAD, consistency 0 FAIL/0 WARN, 44 tooling tests, 69 roots/303 routes/79 root aliases/99 routing aliases/140 features and zero report-mode blocking findings. Actual Trash logs were read: Paper direct `102246707149`, Folia direct `102246707371`, Folia cooperative `102246707437`, Paper cooperative `102246707698`; all show real reload, startup and shutdown PASS, BUILD SUCCESSFUL, and cooperative cases explicitly have `connectedPlayers=0`. These are not client HUD or connected wall tests.

IceSMPGuides mirror is published at `27284ee93864c3f1429f79dcbf22d4ef0163a165`, tree `5a18b4579e49074d1aa61247947de9a002038641`, same feature branch, draft PR #20 based on #19 (`616a69a3e0a475c0f8bfd77dc853cc6380fa9550`). All 14 retained public guides are byte-identical. The #155 tax-free fresh-start ROADMAP clarification is now ported; older unported statements are historical. No sibling branch was rewritten.

This successor extends the existing native history frame/WAL with bounded unresolved projectile-wall operations. Snapshot schema 4 reads schema 3; WAL payload schema 2 reads schema 1 and forbids new wall data under old versions. Consumption and its receipt share one acknowledgement. The receipt contains the actual native field ID/geometry/world/owner/reservation/expiry, projectile UUID, exact before/consumed history revisions and consumption time. Receipt/history mismatches or malformed recovery data fail closed. Capacity is 1024 unresolved operations with no eviction; pending instances refuse later history mutations.

The co-owned runtime consumes through this port, rechecks actual admission after acknowledgement, removes the projectile on its owner and records completion only from its fresh removal observation. False/busy confirmation, expiry/shutdown after acknowledgement or removal failure retains the pending receipt; it is not silently retried/refunded or called successful. The read-only WW item snapshot captures history and pending receipt under one native lock. World inspection includes only its own unresolved operations, at most 32 displayed rows plus exact count and explicit truncation. Expiry does not fabricate completion. Existing archaeology transformation facts describe acknowledged item transformation, not projectile removal; the inspector labels the separate unobserved effect explicitly.

Local source preflight for this successor: 1441 main/regression Java sources compile with zero errors and three inherited warnings. New real native history/WAL receipt regression has 69 assertions and runs inside the existing native inspection task (81 preserved assertions); provider inspection has 248, native fields 194. The native receipt fixtures cover acknowledgement loss after real fsync, rejected pre-append writes, same-frame rollback, restart/compaction, busy/reentrant refusal, stale completion, malformed snapshots/WAL and capacity without eviction. These are storage/pure inspection proofs, not real connected interception. Exact successor GitHub build and smoke results remain required after publication.

**First next implementation task:** finish observable native pending-operation recovery and the cross-region owner protocol using these canonical receipts. Physical inventory/playerdata reconciliation, late/busy confirmation recovery, crash/removal failure outcome assessment, owner retirement/unload/disable closure and full native influence admission remain incomplete. The existing co-owned restriction stays until the replacement is implemented and verified. Do not register developer wall creation yet. The receipt foundation does not close any complete-domain coverage blocker.

Then extract the shared anomaly/relic activation APIs, implement all seven native-backed developer Trash actions and their durable adapter/Undo/recovery/prototype/influence contracts, finish WW04 universal ingress and remaining WW06–WW09/full-provider scope. The normative design, existing canonical seams, evidence-required gates and archive-only restrictions below all remain binding. Do not redo the lifecycle/scanner/native receipt repairs or create a second history/field authority.

Remaining external gates: 18 authored NPC bindings are missing on the reported dev server; actual deployed JAR/hash is unavailable; connected two-region client/HUD/resource-pack and populated Paper/Folia/multiplayer/builder/human acceptance are unproven. Mandatory NPC fail-closed and equipment audit warnings remain intact. Full source scope is still unfinished and there is no merge-ready/production-ready verdict.

## Active instruction update — findings before full WW implementation

The user explicitly resumed the previously deferred command/disable/HUD/reload findings and asked to fix them first, then complete the full WW implementation. This supersedes every older "deferred HUD" execution-order sentence below. No merge/release/production enable was authorized. The public starting HEAD for this repair is `8d79bd9ca44a72ef2eb1c9a147402d2a1b25ba9f`, rechecked against PR #159 immediately before publication. Full WW scope and all 51 coverage blockers remain unchanged.

Current repair adds the native Paper BasicCommand lifecycle guard at the actual JavaPlugin registration override; closes execute/suggest/canUse before core teardown; caches permission without re-entering an unloaded command; drains entered callbacks on the cooperative fail-closed route. Native resource-pack delivery records the actual offered layer IDs, retains uncertain sends/removals, refuses stale sends after close, and removes only its own layers on player owners before plugin scheduler retirement. Core preparation/final profile teardown are separated; existing durable save/drain gates are retained; HUD/tab callbacks are fenced before owner cleanup. Final teardown no longer calls the foreign-player `ProfileGUI.closeAll` loop. NPC readiness, moderation and invsee fail-closed triggers use the cooperative route. Forced external disable and timeout/failure log incomplete cleanup truthfully; connected-client restoration is not claimed.

Meaningful local evidence: 1440 Java sources compile against the 49 cached Java 21 dependencies, zero errors/three inherited warnings. Actual closed-JAR late command body regression and entered-callback drain: 29 assertions; native delivery failure/race cleanup: 31; Profile teardown ordering/resources tests pass; existing lifecycle, resource-pack, HUD, editor, config startup, moderation and affected Trash/provider suites pass. Coverage 339 authorities/55 domains/51 implementation blockers/0 audit errors; PlayerProfile guard 695 classified/0 transitions; architecture/resource helper/sprite and consistency checks pass. Full local wrapper remains blocked by unreachable Gradle distribution network. New exact-HEAD GitHub build remains required before claiming full build PASS.

The existing Paper/Folia smoke probe keeps the direct server-shutdown case and adds a separate cooperative-shutdown matrix case. Both invoke the real `/icesmp reload` through the server dispatcher and verify snapshot advancement plus packaged `content/progression/classes.yml` readability. Cooperative proof checks actual closed/drained command admission, native preparation completion and final Profile authority retirement. These new CI cases are not yet evidence until their actual exact-HEAD logs pass; zero connected players cannot prove live client pack removal or multiplayer ownership behavior.

Public admin acceptance and ROADMAP are updated; the later #155 tax-free fresh-start ROADMAP clarification is now ported verbatim without a gameplay change. The 14 retained public documents are byte-mirrored in a dedicated IceSMPGuides feature branch based on its current #19; do not copy internal WorldWeaver documents to that repository. Final published SHAs/PR and CI outcome must be recorded after publication.

Remaining finding gates: actual missing 18 authored NPC/world bindings; inspection of the user's deployed JAR/hash for the reported packaged-authority error; connected two-region client/pack/HUD acceptance and external-disable fallback observation. Equipment audit warnings remain intact; no balance exception was invented to silence them. Full cross-region wall durable receipt/recovery, shared activation APIs, all seven developer Trash actions, WW04 ingress completeness and WW06–WW09 remain implementation work. Continue them after resolving any new CI failures in this repair.


**INTERNAL DEVELOPER ONLY.** Primary continuation authority; not a completion, merge or release report. This supersedes the earlier same-date smoke/inspection resume and the `63d401e` documentation snapshot. Smoke and inspection are complete, and the native wall safety/fence/admission repairs below are now published. Do not redo them. Full cross-region wall semantics, the complete Trash provider and WW-00–WW-09 acceptance remain unfinished.

## Goal, authority and immediate continuation

Continue the full Világszövő / WorldWeaver implementation for `MilCsik09/IceSMP`. The normative specification is `docs/development/world-weaver/design-v2.md`; read it in full before implementing. The original user requested the complete WW-00–WW-09 scope, reviewable stacked phase PRs, dynamic providers/content discovery, zero unresolved coverage blockers, Folia safety, durable recovery and zero SANDBOX reward leakage. Do not cut this to an MVP, admin wand, command registry or scripting system.

Current branch: `feature/world-weaver-ww05-trash-territory`; open draft PR [#159](https://github.com/MilCsik09/IceSMP/pull/159). Last source-changing and published HEAD: `7375034adf59a2d4a41f6172b945a748d8b36f2e`, exact tree `c08ba3e077cb6a97940f20cd43c09e2f2870f682`. Parent source checkpoint: `60da0dff2d4dc03c3c0732354fc5502df608255a`, tree `1714203ea33a85cc35fb127dfda35cdfc81ad0cb`. This resume is intended for a documentation commit directly above `7375034`; resolve that final documentation SHA from the live branch/PR. Resolve the actual public branch HEAD before working; never reset a newer commit to this snapshot.

The first next implementation task is the remaining native projectile-wall durable consume/remove receipt and recovery gap, including cross-region owner execution and shutdown after admission, described below. The dangerous freeze/foreign-handle path is removed; do not recreate it. The current safe runtime deliberately refuses foreign-region interception. This is a temporary capability restriction, not a design waiver. Then extract shared native anomaly/relic activation APIs, implement remaining Trash mutation/history/prototype/recovery/influence paths, and continue WW-06–WW-09. Do not confuse this inspection checkpoint with a reduced final scope.

Source priority: current public repository/remote HEAD → current AGENTS.md and repository instructions → canonical/normative design → this primary resume → current evidence/coverage → backlog/checklists → historical audits/checkpoints. Inspect later explicit decisions when semantics conflict; never invent a compromise.

Read before implementation:

1. This primary resume in full. Other WW documents are supporting design/evidence, not competing session resumes.
2. Root `AGENTS.md`, any applicable nested AGENTS.md, and `docs/ARCHITECTURE.md` Folia ownership rules (§4). Preserve CLAUDE.md compatibility if changing durable instructions.
3. Entire `docs/development/world-weaver/design-v2.md` (normative v2, 2938 lines at this checkpoint).
4. Entire current `WW-00-coverage-audit.md`, `WW-00-ci-audit-closure.md`, all WW-01/WW-02/WW-03 evidence and cumulative-refresh files, all `WW-04-*.md`, and `WW-05-territory-policy.md` in the same directory. Review `coverage.json` domain, authority, bootstrap and hash entries. Chronological checkpoints supersede only the specific later implemented gaps; early “not implemented” snapshots are not the current runtime inventory.
5. Current remote branch, PR dependencies, latest workflows/job logs, newer commits and parallel cumulative work. The current session fully read those design/evidence inputs and verified public state; a future session must repeat the freshness check.

Commit/push to the designated feature/stack branch and internal developer documentation remain authorized. Do not ask again. No merge, release, production enable, master/staging work, CI weakening, invented world coordinates/bindings, fake events, receipt/history spoofing or archive restoration is authorized.

## Current public stack and newer cumulative change

All phase PRs were open, draft and unmerged when checked. Phase existence does not mean acceptance.

| Phase | PR | Branch | Public HEAD at inspection |
| --- | --- | --- | --- |
| Native Trash base | #152 | `feature/trash-production-hardening` | `a335b3b5acaea66772534e51527a1c9233a85d1a` |
| Cumulative gameplay | #155 | `feature/faction-crime-whisperer-rework` | `0cd465639e73f3c2e8dee1cfe107c97f010d1f8d` |
| WW-00 | #153 | `feature/world-weaver-ww00-coverage` | `0a3cd260260042e91d79b638510239cc18e2c59c` |
| WW-01 | #154 | `feature/world-weaver-ww01-artifact` | `178e186a6421df98549a3915c4b33bba924a4678` |
| WW-02 | #156 | `feature/world-weaver-ww02-kernel` | `d57fff9f53ab57b3ce557d20a24c2821d891b3c5` |
| WW-03 | #157 | `feature/world-weaver-ww03-durable` | `17d985dc3a9d8fe62e11ee907efced823bc67689` |
| WW-04 | #158 | `feature/world-weaver-ww04-pve-faction-integrity` | `ac6e46eb96a6d4cd8b00e778afb775acdeb2ae4c` |
| WW-05 | #159 | `feature/world-weaver-ww05-trash-territory` | `7375034adf59a2d4a41f6172b945a748d8b36f2e` before this documentation commit |

Dependency chain: #152 → #155 → #153 → #154 → #156 → #157 → #158 → #159. WW-05 phase base stays #158 at `ac6e46eb96a6d4cd8b00e778afb775acdeb2ae4c`, tree `373e4c9769097e3679397dbcf43287133ec6b59a`. WW-06–WW-09 branches are not yet created in this work; WW-10 enable/merge remains a separate user decision.

The inherited WW stack contains gameplay #155 through `2a78eb6071db063740ce1f8e6889b03d9741dbb0` (tree `6389054ac358fdddb066c665dcee9d6acab4679e`). The newer #155 commit `7cdea83` changes only Pillow pixel access in `scripts/process_trash_sprite_sheets.py`. Its actual diff was reviewed against this branch: the WW stack already has independently tested histogram/getcolors handling and six negative pixel fixtures. Do not blindly overwrite that implementation with the alternative accessor patch. No newer gameplay difference was found in this cumulative update. Recheck before deciding any later refresh. #155 had an unrelated docs fixture-cleanup failure (`OSError: Directory not empty` under a temporary `.git/objects`); this branch's own CI is separate evidence.

A final freshness check found a further #155 advance to `0cd465639e73f3c2e8dee1cfe107c97f010d1f8d`. Its complete three-file diff was reviewed: `scripts/tests/test_inventory.py` and `test_release_inventory.py` disable automatic maintenance/gc only inside temporary fixture repositories, preserving all assertions and strict cleanup; `ROADMAP.md` removes the obsolete tax-collection recovery acceptance clause and explicitly retains paid faction-switch WAL/recovery tests for the approved tax-free fresh start. There is no Java/gameplay change. The two exact temporary-Git-fixture fixes are NOW ported in `60da0df`, and their 13 tests pass with assertions and strict cleanup preserved. The ROADMAP-only edit remains unported; changing that public document still requires its normal mirror update. No Java gameplay difference exists in those newer sibling changes. Treat the later explicit tax-free/fresh-start decision as authoritative, not the older ROADMAP clause. Before a cumulative refresh, inspect any further advances, retain the already ported exact fixture changes and stronger existing sprite implementation, and obey public-guide mirror requirements if changing ROADMAP. No sibling branch was rewritten or merged.

## Earlier completed smoke/inspection checkpoints — historical, preserve

1. Reconstructed an isolated worktree from actual public #159 HEAD `12c5195e0913d54b4232a5d31de6638136b6c4f8`. Older dirty worktrees and archive branches were untouched.
2. Published `cf93e05847c6f02bdcf4f0fb4b18b7e785f15321` (tree `b832adfd91dd1f66d20996e6ec4670bc59f1fc25`): TrashProductionRuntimeProbe now resolves the assembled TrashRelicRuntime and reads its canonical `ruleFields().snapshot()`. Startup requires open/empty fields/empty claims; shutdown requires closed/empty fields/empty claims. Negative regression fixtures independently reject residual fields/claims and wrong lifecycle state. No smoke marker was skipped or weakened. This closes the original first blocker.
3. Published `8a9bc6647abdca508b359041d002bba6b95f1387`: native nonblocking Trash inspection, canonical marker validation, pure archaeology derivation, generic read-only provider registration, dynamic catalogs, meaningful storage/provider tests and stronger real Paper/Folia detached-item smoke checks. Detailed changes follow.

## Historical inspection source CI and evidence limits

Source HEAD: `8a9bc6647abdca508b359041d002bba6b95f1387`.

| Workflow | Run | Verified result |
| --- | --- | --- |
| WorldWeaver verification | [34202573875](https://github.com/MilCsik09/IceSMP/actions/runs/34202573875) | SUCCESS |
| Trash production hardening | [34202573908](https://github.com/MilCsik09/IceSMP/actions/runs/34202573908) | SUCCESS |
| Resource-pack validate-only | [34202573818](https://github.com/MilCsik09/IceSMP/actions/runs/34202573818) | SUCCESS |
| Repository Docs Inventory | [34202573873](https://github.com/MilCsik09/IceSMP/actions/runs/34202573873) | SUCCESS |

Actual Trash logs were decoded and read: Paper job `101984541057`, Folia job `101984541075`, both native `ICESMP_TRASH_PRODUCTION_RUNTIME_PROBE_PASS`, clean plugin disable, `ICESMP_TRASH_PRODUCTION_RUNTIME_SHUTDOWN_PASS` and BUILD SUCCESSFUL. Java 21 Phase G job `101984540929` also succeeds. WorldWeaver full build job `101984541099` was also read: both new regression tasks execute (37 and 175 assertions), followed by `:build` and BUILD SUCCESSFUL. All five WorldWeaver jobs succeed. The additional Trash checks run before the existing startup marker and therefore passed on real Paper/Folia 1.21.11: detached ItemStack markers, fresh-stack byte identity, native history writes/reload, stale/duplicate/partial/wrong-type/lifecycle rejection and non-awarding archaeology derivation. This is not connected-player, multiplayer, populated cross-region, GUI/client rendering or human-playtest proof.

The earlier smoke-fix source `cf93e058` also passed all workflows: WorldWeaver `34200127377`, Trash `34200127389`, resource pack `34200127361`, docs `34200127390`. Its Paper/Folia lifecycle logs were read. The old aeb74df/12c5195 smoke failures are historical, not the next task.

Historical local source verification at `8a9bc66`: full main+regression Java 21 javac against 49 actual dependency jars, zero errors, three inherited deprecation warnings. All nine native Trash suites plus the provider suite pass, including 30,000,000 loot-distribution events. New native inspection suite: 37 assertions; new provider inspection suite: 175; native fields: 194. New suites are normal Gradle check dependencies. Architecture 4 tests pass; consistency 0 FAIL/0 WARN; PlayerProfile guard 692 classified with zero unknown/stale/invalid/transition findings. Coverage: 339 authorities, 55 domains, 51 implementation blockers, zero audit errors. Main sources: 1251.

The local wrapper build was attempted but distribution access was network-blocked. The supplied Java 21/Gradle 9.4.1 offline build was also attempted; run-paper plugin 3.0.2 is not cached. No source exclusion, stub dependency, shared-cache deletion or gate weakening was used. Local javac is preflight; the actual GitHub Gradle build is authoritative. Do not label javac alone a full Gradle/runtime PASS.

## Native Trash inspection now implemented

- TrashHistoryStore and TrashAnomalyStateStore use one ReentrantLock each, replacing their intrinsic monitor across existing public serialized methods. Native maps, WAL and YAML remain the authorities. New `tryInspect` uses immediate tryLock, refusing contention and reentrant reads during an active write. No second cached state/publication framework was introduced.
- Loaded absence differs from unavailable/unloaded/uncertain state. The history snapshot carries native sequence and immutable per-instance history. Failed/uncertain write and failed load keep inspection unavailable until explicit successful native load assessment. Expected validation refusal before entering a disk write rolls back and retains the acknowledged view.
- Anomaly durable writes now roll working state back on CriticalPersistenceWriteError (Error), in addition to RuntimeException. Real failure injection tests before and after actual atomic replacement prove that load observes actual disk state without replay. Runtime memory may include unsaved WATCHED_TICKS; inspection explicitly does not claim restart durability.
- Existing ordinary mutation APIs may still hold the native lock during I/O. The new inspection ports do not prove all native activation/write paths are nonblocking or ready for developer execution.
- TrashHistoryService.tryInspect owns physical item validation and returns detached ItemInspection. It checks singleton/current tracked revision, base/lifecycle, origin, malformed/partial markers and pending repair. Present-but-wrong-type or blank marker values cannot be mistaken for absent fresh state. Untracked base stacks allocate no UUID/history. Providers never interpret raw PDC.
- TrashArchaeologyFactEngine.evaluate(definition, optionalHistory, level) is a pure overload; ordinary evaluation delegates to it. The provider derives level-50 potential facts without awarding discoveries, progression, knowledge or currency, and without claiming the holder's learned level. No DEV_PROTOTYPED event or prototype was added.
- TrashWeaverProvider registers at the composition root for ITEM_SLOT and WORLD. Player-slot access checks ownership, online status and the canonical WeaverItemSlots fingerprint before reading. Inspect/discover/frontend use only detached facts. It shows identity/kind/behavior, native anomaly primitive when defined, lifecycle, instance/revision, origin, all retained history, owners, derived archaeology and native runtime memory. Native field views retain real world UUID/coordinates/radius/expiry/owner/claim state and scope to one world; inspection does not expire fields or take claims.
- Identity catalog `icesmp:trash_identity_ref@1` is generated from TrashCatalog snapshots; `icesmp:trash_rule_ref@1` uses the native FieldKind enum. No hardcoded identity roster or frontend/domain switch was added. Maximum 64 history entries/64 owners and 32 world-local fields fit existing value/facet caps.
- Separate native-store observations are not an atomic cross-store transaction. No mutation admission uses these read-only snapshots. Connected owner handoff/GUI observations are still evidence-required.
- Registered coverage is only `trash.inspection_subset` INSPECT_ONLY_BY_DESIGN. The WW-00 `trash` domain retains DEFERRED_BLOCKER and FULL_PROVIDER target; none of the 51 blockers was renamed away. No actions/import/export/Undo/recovery receipts/prototypes are fabricated. Complete Trash is NOT implemented.

## Current continuation: completed native safety, durability and admission repairs

Published `60da0dff2d4dc03c3c0732354fc5502df608255a` (13 files) removes native projectile freezing and foreign-owner Projectile/Vector continuations. The actual wall callback reacquires its reservation owner by UUID, requires current Folia ownership of BOTH entities, online/same-world/valid entity state, open tracking and the exact active field claim, then commits the canonical reserved-brick transform before native projectile removal. Foreign-region owners are refused with no brick consumption or projectile physics change. Therefore a wall whose owner moves into another Folia region currently **does not intercept**. This safety containment does not implement the required full cross-region behavior.

The real runtime uses bounded detached identity tickets in `TrashRelicPolicy.ProjectileTracking` (256 cap). Duplicate admission, old-callback ABA, scheduler null/rejection, retirement, timeout, tick failure and shutdown are handled without storing Bukkit entities or creating a second field authority. Field claims release in finally, including critical persistence errors. World unload removes native fields; player cleanup removes native wall authority before owner-correct reservation-marker cleanup. A post-acknowledgement marker-cleanup RuntimeException cannot turn successful consumption into a retry. Rejected cleanup after disable leaves only non-authoritative markers for existing startup/join/use reconciliation. The real runtime probe verifies typed empty/open and empty/closed tracking snapshots alongside native fields; negative fixtures reject incorrect state.

The same source commit fences mutations AND saves in native history/anomaly stores after uncertain writes, not only read-only inspection. Only successful native load assessment reopens them. Real WAL append+fsync followed by lost acknowledgement, and real anomaly atomic replacements before/after failure, prove that subsequent mutation/save cannot overwrite the durable candidate. Native WAL/YAML remains the sole authority. No replay or fabricated receipt is used. Native inspection/durability assertions increased from 37 to 65 at this checkpoint.

Published `7375034adf59a2d4a41f6172b945a748d8b36f2e` (seven files) adds `TrashHistoryStore.tryTransact` on the existing canonical lock/transaction. It refuses busy or reentrant entry immediately with untouched callbacks, requires loaded acknowledged state, checks live admission before native compaction and again immediately before mutation after compaction. `TrashHistoryService.tryTransformInventorySlotOnSuccess` shares the ordinary exact-slot transform/rollback implementation. The actual wall consumer supplies current tracking/owner/world/field admission to this native port. This avoids waiting behind another owner write and then consuming on a stale pre-wait decision. **The admitted WAL/compaction I/O is still synchronous**; this is non-waiting lock admission, not a fully asynchronous activation API.

Native inspection/durability now has **81 passing assertions**, including actual held-writer/reentrant refusal, original projection failure and external rollback, unloaded/uncertain fencing, and 1024 real fsynced WAL entries causing actual compaction. A predicate that becomes false when the snapshot appears prevents candidate projection after compaction, while exact acknowledged history reloads. Affected history/relic/hardening/field/provider suites pass; provider remains 175 assertions, fields 194. All 1437 main/regression Java sources compile against 49 real Java 21 jars with zero errors and the same three inherited warnings. Main sources remain 1251. Coverage: 339 authorities, 55 domains, **51 implementation blockers**, zero audit errors; profile authority 694 classified with zero unknown/stale/invalid/transition findings; architecture four tests; consistency zero FAIL/WARN. Earlier in this continuation all nine Trash suites plus provider, 30M distribution events, 18 resource-pack helper tests, six sprite tests and 13 inventory fixture tests passed. No source/test/CI gate was weakened.

Inventory report mode still records 69 roots, 303 routes, 79 root aliases, 99 routing aliases, 140 features and 75 blocking inventory findings. This is not strict release acceptance; do not confuse those report findings with the 51 WW implementation blockers or report zero blockers. Preserve all strict CI gates.

## Current checkpoint CI and precise runtime scope

All four exact-head workflows on `60da0df` succeed: WorldWeaver [34268750955](https://github.com/MilCsik09/IceSMP/actions/runs/34268750955), Trash [34268751187](https://github.com/MilCsik09/IceSMP/actions/runs/34268751187), resource pack [34268751044](https://github.com/MilCsik09/IceSMP/actions/runs/34268751044), docs [34268750957](https://github.com/MilCsik09/IceSMP/actions/runs/34268750957). Decoded full build job `102204690716` runs strengthened relic tests, inspection 65/provider 175 and BUILD SUCCESSFUL. Actual native Trash Folia `102204691106` and Paper `102204691254` logs contain startup PASS, disable, shutdown PASS and BUILD SUCCESSFUL. These are assembled empty-tracker/native-field lifecycle and detached-item/history checks, **not connected projectile-wall, populated-region, world-unload traffic, client HUD, multiplayer or human evidence**.

The prior documentation HEAD `63d401e` also has all four workflows successful: WorldWeaver `34203399177`, Trash `34203399172`, resource pack `34203399287`, docs `34203399235`. The older handoff/PR statement that docs was still running is superseded.

New source `7375034` has separate runs: WorldWeaver `34270729024`, Trash `34270728809`, resource pack `34270728898`, docs `34270728943`. At documentation publication, Trash and resource-pack workflows succeed; WorldWeaver and docs remain in progress. The WorldWeaver full-build step already succeeds while evidence export continues. Actual Trash Folia job `102211359236` and Paper job `102211359248` logs were read and contain startup PASS, disable, shutdown PASS and BUILD SUCCESSFUL, with the same limited empty/detached scope. Recheck the remaining results and final documentation HEAD; never transfer a different SHA's result. The PR and exported session resume record the final observed result. Local wrapper/installed offline Gradle limitations below still apply; real GitHub Gradle is authoritative.

## First next blocker: complete native projectile-wall receipt/recovery

Source: `src/main/java/hu/taliann/icesmp/trash/TrashRelicRuntime.java` onProjectileLaunch, resolveProjectileWall, consumeBrickReservation, clearPlayerState and shutdown; native TrashHistoryStore/Service and TrashRuleFieldService. The old restoreProjectile/frozen-handoff path no longer exists. **Do not restart the completed safety/fence/admission fixes or present the co-owned restriction as full behavior.**

Still unimplemented: recoverable cross-region consumption/removal; a native durable operation receipt linking exact reservation/history acknowledgement to the observed projectile effect; crash or projectile-removal failure after acknowledged consumption; shutdown concurrent with already admitted commit; complete native influence admission. Current final predicate checks narrow lifecycle races but do not close the after-admission/after-acknowledgement window. A committed brick followed by failed removal is not a successful interception and cannot be safely retried by inventing another receipt. The ordinary native write path still performs synchronous I/O; account for owner-safe projection and observation when designing the shared seam.

Repair at the existing canonical native activation/history authority, with bounded detached identities, real owner-correct reacquisition, durable operation identity, observable effect outcome and actual retirement/rejection/unload/disable recovery. Read normative §33 and the existing WW durable protocol before choosing the concrete integration. A fixture-only ledger, second field/history authority, invented reservation token, fake Bukkit event or damage replay shortcut is forbidden. Do not remove projectiles before native consumption acknowledgement. Do not register developer wall creation before native handoff/lifecycle AND influence closure. Real connected/cross-region tests remain separately required after implementation.

Then continue shared TrashAnomalyActivationService and TrashRelicActivationService extraction: ordinary runtime and developer routes must use the same canonical behavior APIs. TrashRuleFieldService already exists; retain it rather than rebuilding it. Required actions remain `trash.individualize_unit`, `trash.activate_behavior`, `trash.transition_success`, `trash.repair`, `trash.give_sandbox_copy`, `trash.create_rule_field`, `trash.remove_rule_field`. Canonical history/transform/repair exists, but developer durable adapter admission/receipts/Undo/recovery and prototype/influence integration do not. Developer-created evidence must never fabricate natural archaeology; if adding DEV_PROTOTYPED, explicitly exclude it from natural evidence. Persistent field Fork is WW-08.

## User-reported live-disable/HUD/reload findings — explicitly deferred

The user supplied actual Folia logs in this session. At +60 seconds the mandatory FancyNpcs snapshot validation reported **required=18, available=0, missingOrMismatched=18, lookupErrors=0**, then the existing fail-closed path disabled IceSMP. This is the logged disable cause; no Trash causality or corrupt playerdata was proved. Required exact internal names are in native quest references; no coordinates, world, NPC placement or binding may be inferred. Deleting playerdata cannot create these authored NPCs, and the gate must remain fail closed.

The user also reported `/hud` reaching uninstalled PlayerProfileAuthority; command completion reached `zip file closed` in the plugin classloader. Source review found no common closed-lifecycle guard around BasicCommand execute/canUse/suggest. ResourcePackListener.close clears server capability/development resources but does not remove its resource-pack layer from connected clients. The logged native-HUD-fallback message is not connected-client restoration proof, and the user observed missing vanilla HUD. Owner-correct pre-disable cleanup and late command/classloader protection remain unimplemented. A provisional untested command guard was fully reverted; **no HUD/command/disable fix was committed or published**.

The user explicitly said to leave this investigation for now and continue WW blockers. Respect that steering: keep these findings visible, but do not resume them ahead of WW without a later request. Do not promise that finishing Trash will resolve them.

Later `/icesmp reload` output contains Equipment 2.0 DISFAVORED_STAT/CLOTH_ARMOR_OUTLIER warnings and `Missing packaged authority: content/progression/classes.yml`. The reload rejected its candidate and retained the previous snapshot. That resource exists in current source and is not excluded by processResources. A closed classloader is consistent with earlier errors; the deployed JAR and hash were not inspected, so its contents/state are unproved. `/pl` text listing does not prove enabled state. Do not mute audits or invent a successful reload. This dev environment has no complete NPC readiness and supplies no WW/client/playtest PASS.

## WW-05 Territory checkpoints already published

Detailed design mapping and checkpoint-specific limitations are in `WW-05-territory-policy.md`. Earlier paragraphs are chronological; later checkpoints supersede only the specific implemented gaps.

| Checkpoint | Commit | Principal evidence |
| --- | --- | --- |
| Shared native protection policy | `8be262e05a29f8f26d53416fe62fafa43b580825` | 419 policy assertions; true native handlers and regeneration precedence |
| Native canonical transaction | `5fb86b1ddb1ecfb15fbb5aa1950b3302831ad4b0` | 83 real-YAML assertions, atomic receipt/state publication, fault injection |
| Bound projection provider | `85e6c45f232e7980799e60ae6f11d8492d47ac8e` | 86 assertions, dynamic catalogs, Thread, projection consumer and crash boundaries |
| Canonical provider adapter | `3af3bd0816793dd06ac22a2bd98684d411c72a13` | 94 assertions, four LIVE_GM actions, durable-before-I/O, observed recovery and Undo |
| Local Subject inspection | `be23de1332814b91e97c4b33cdf2b116ac71754d` | 59 assertions, PLAYER/ENTITY/BLOCK/LOCATION detached trace capture |

At `be23de1`, all four workflows succeeded: WorldWeaver `34154553019`, resource pack `34154552965`, Trash `34154552954`, docs `34154552893`. This is historical pre-field-extraction evidence; newer successful source checkpoints are recorded above. Do not restore it over the current stack.

TerritoryManager now stages immutable zones/spawns/chunk index/native receipts, atomically writes YAML, then publishes after acknowledgement. Uncertain writes retain the acknowledged live snapshot and fence writes until load assessment. Polygon geometry is defensively copied. Native `adjustConditionally` supports rename/type/owner/Y-bounds with expected SHA-256 fingerprints, exact operation/request/native receipts, replay acknowledgement without replaying mutation, explicit capital collision and a bounded 4096-receipt ledger. Undo creates a new compensating native history entry. Provider I/O runs on AsyncIoOwner after PREPARED with final authority/world fingerprint admission.

TerritoryWeaverProvider registers via the existing composition-root provider factory. WORLD actions include projection, sever/clear, conditional Undo, typed rule Thread and four canonical actions. Native enums/zones back dynamic catalogs; no core/artifact/GUI domain branching was added. The native protection service consumes projected rules through circuit isolation. Canonical claims, ownership, treasury/tax/history and reward identity do not consume projections. ALLOW, including sever that reveals an older ALLOW, requires LIVE_GM. SANDBOX world influence is published atomically with projection and is not washed by sever, Undo or restart.

Local PLAYER/ENTITY/BLOCK/LOCATION facets inspect typed native zone metadata and ordered decision traces. Live handles remain in owner callbacks; coordinate subjects require already loaded chunks; drift is rejected. Context is explicit player-actor or environmental, with no invented PvP victim. WORLD mutation actions are not exposed against local subjects. Generic local-to-world navigation remains WW-07 work. Populated/cross-region protection, world lifecycle and client evidence remain required.

## Foundation already present — preserve and extend

Source root is `src/main/java/hu/taliann/icesmp/dev/weaver/`, not `.../worldweaver/`. Runtime authority adapters also touch native subsystem packages.

- Existing DevItemManager/DevItemFactory were generalized for the physical developer artifact; no second DEV lifecycle framework. Durable singleton identity, duplicate/forgery checks, recovery and four modern resource-pack states exist. Bingulus remains separate.
- Primary-developer UUID is `2d47d7b6-294e-4a14-922c-befacd66ee6d`; HiddenDevAuthority is the boundary. OP, admin and permission checks cannot replace it.
- Generic seven SubjectRef kinds; typed, namespaced/schema-versioned YAML-safe values; provider/coverage/type/catalog contracts; descriptor-only GUI, dialogs, confirmation, history/diagnostics, Thread Case/Recent/Quick Apply; five bounded AREA geometries and continuation-based owner routing.
- Durable PREPARED/APPLIED/COMMITTED protocol, atomic receipt/projection/influence publication, ABORTED/COMPENSATED/NEEDS_REVIEW states, observed recovery, unavailable-entity pending/load wakeup without force-loading, scoped revisions and conditional Undo. Entity/item/event influence is monotonic; player/world/spatial tail is retained. Unknown providers/content remain opaque and fail closed.
- Recovery of a caught ambiguous entered-stage failure remains NEEDS_REVIEW; automatic recovery targets PREPARED/APPLIED. Manual developer review controls are not finished. Never force-retry or manufacture a receipt.
- Provider circuit isolation, bounded queues/operations/receipts/areas, retirement/shutdown handling and journal retention pins exist. Dynamic fixture tests demonstrate registry content and dummy-provider discovery. That does not establish universal subsystem completion.

Current composition registers MinecraftWeaverProvider, PvEWeaverProvider, FactionWeaverProvider, TerritoryWeaverProvider and the read-only TrashWeaverProvider inspection subset. **The complete Trash provider and its mutation routes remain unimplemented.** Reward-gate adapters for a domain are not the same thing as its generic developer provider.

Production execution remains disabled: the WorldWeaverRuntime composition passes `() -> false` to the execution coordinator. Retain the existing artifact/production gates until separately authorized.

## WW-04 implementation and unresolved integrity work

Read all relevant `WW-04-*.md` documents in this directory before changing these paths. They contain exact source/checkpoint/test evidence, not universal PASS verdicts.

Already present:

- PvE registry-backed ability/template/rank/archetype catalogs, typed Threads, session/persistent projections and the actual native projection port; canonical loot/Bestiary identity retained. Force ability uses the real cast lifecycle and validated target/cooldown/cast epoch. Runtime control ledger and bounded recovery exist.
- Faction temporary membership/entity semantic contexts use canonical passive/target policy; CROWN_CURSE remains native. Canonical membership uses native conditional profile WAL, real receipts and pending cleanup before benefits. Native guild/council/king/raid cleanup ordering and offline/online recovery are guarded.
- Neutral reward eligibility and effect/source bindings are exclusive and fail closed. Several kill/drop/XP and class/pet/Bestiary/Quest/community/currency beneficiaries are gated; earned WAL/outbox replay remains valid.
- GameplayEffectContext carries bounded original sources/targets. Durable source/target influence precedes a single-use owner-revalidated effect permit; actual lifetime observers retain potion/other influence until observed end plus quarantine tail. Provider failure/unavailability cannot prematurely clear influence.
- Known native parent/source UUID propagation precedes unknown child creation; actual authored/summon origin APIs and causal source capture are bound. Native PvE arrows/summons stay inert until permitted. Owner callbacks and immutable causal context replace several foreign entity reads.
- Server challenges use real run UUIDs and original lineage. Community/season credit has native final gates and real earned outbox handling. Quest progress/completion uses canonical WAL and receipt paths; queue lanes bound and serialize mutations. Statistics/profession/knowledge earned transitions have additional final gates and regressions.

Still required; do not declare zero reward leakage:

- Comprehensive native damage/projectile/DOT, player/item/event/spatial/world influence propagation, including crossing into/out of world/spatial quarantine.
- Remaining shared season/raid/war/duel/spy/bounty/crate/personal-loot/parkour/currency-faucet routes and their original causal sources/final gates.
- PvE stats/health/resistances/source-combat contexts, AREA/cross-region capabilities; Faction scripted target/peace and remaining effective passive surfaces.
- NPC quest hand-in currently removes items before async progress settlement; physical reward outbox and chain/spec/sins/spell/guild followups need closure.
- Hidden discovery first-finder/item outbox, community synchronous completion I/O and ephemeral server-challenge reward outbox remain.
- No general safe synchronous damage admission/replay solution was implemented. Cancel → persist → replay damage loses/doubles native critical/sweep/enchantment/knockback semantics. Do not use fake events or copy unsafe live-handle damage replay. Foreign player UUID alone does not capture world/spatial causal sources. Audit concrete handlers rather than claiming a global shortcut.

Earlier fixture assertions include effect propagation 214, observed influence 157, causal sources 51, influence persistence 162, server challenge 125, profession 74 and knowledge 133 at their documented checkpoints. Do not present these as newly rerun results for this handoff.

## Remaining phases and acceptance

- WW-06: actual Itemization/Event/DeveloperSelf/Knowledge providers through safe native APIs; explicit arming, vanish via VanishManager, artifact recovery/session cleanup and manual NEEDS_REVIEW/unknown-content controls.
- WW-07: multi-field Imprint/Impress whitelist, full Thread/AREA selection UX and generic subject navigation. Do not imprint UUID, owner, event/reward/economy receipts, immutable history or profile revision.
- WW-08: bounded SANDBOX-only Binding (TTL/uses/cooldown/lineage/loop/caps) and provider-owned Fork prototypes for PvE, Trash rule fields and territory overlays; no arbitrary clone, event lifecycle fork or scripting.
- WW-09: close all current-domain coverage, including class/spec/talents/resources/cooldowns, profession/economy/pets/moderation/challenges/current Doom and anything newer in the cumulative repository. Biome was absent at the audit, not an excuse to miss a newly added subsystem.
- Final acceptance: coverage blockers 0, complete design → implementation evidence audit, required regression/resource CI, real Paper/Folia and explicit connected-client/human evidence gates, no known C0/C1 finding, pushed merge-ready stack. No automatic production enable/merge.

Risk, integrity mode and lifetime are separate axes. Projections require concrete effective consumers. Canonical mutation must remain in each subsystem's native authority or explicit projection consumer. Undo refuses external drift. No force undo, reflection debugger, raw YAML/PDC mutation, fake events, receipt spoofing or legacy scheduler. No permanent HUD or public player/admin/permission documentation for WorldWeaver or hidden Trash surfaces.

## Recovered local work and archival checkpoints

The previous environment disconnected on 2026-09-07 after the active WW-05 worktree had been reset-soft to the published source commit and verified clean. No source edits were made afterward. On 2026-09-08, the filesystem became accessible but had restored older WW-03 and WW-04 worktrees and no WW-05 directory. Their modifications differ from the newer cumulative code. To obey the user's request to commit/push all work without overwriting the newer stack, both exact older working trees were preserved on separate checkpoint branches.

| Archive only — do not merge | Remote commit | Exact tree | Captured changed files |
| --- | --- | --- | --- |
| `checkpoint/world-weaver-ww03-local-20260908` | `6cba6e2124839ecb09cb9b07383814f3e45a4569` | `ffa5a100e4fc50e0787eb32edf63ac52bb8b56e7` | 22 |
| `checkpoint/world-weaver-ww04-local-20260908` | `46f51c8ce6df49f8a23ac740c804f3afdf842bf7` | `aaaa44245b2b63420fb4b479b3c891740a087699` | 15 |

WW-03 archive parent is `14ed612f811b03c5aef7a8c615dd35eb618b867a`. It captures the older AREA/kernel execution work and the exact locally modified `dev-assets/trash/source/batch-004.png`. This old image is a preservation artifact, **not a replacement for the repaired current asset pipeline**. WW-04 archive parent is `f1be063fe5664fc7471a94933ab4b22d90ded9f5`; it captures the older observed-influence work. No build/readiness claim is made for either obsolete snapshot. Their existing user indices/worktrees were not reset or cleaned. Ignored build caches/generated outputs are not part of the source checkpoints.

Remote tree hashes were required to match locally constructed Git trees; blob hashes were also checked. Direct git push lacked credentials, so the already connected GitHub API published the commits/refs. These archive branches have no phase PR and are outside the cumulative dependency chain. Continue from #159, never from an archive. If old paths reappear, inspect before editing and do not blanket-reset them.

## Current workspace, safe verification and publishing

Active fresh clone in this continuation: `/workspace/scratch/bbb9a568eb4c/IceSMP-continuation`, local branch `feature/world-weaver-ww05-trash-territory`. It was verified clean and exactly equal to published source `7375034adf59a2d4a41f6172b945a748d8b36f2e` after exact Git blob/tree checks. This handoff-only documentation update follows it. The old `/workspace/scratch/bbb9a568eb4c/IceSMP` files remained but their worktree metadata pointed at a missing directory; they were not restored over current public state. The uploaded original was read directly from `/workspace/scratch/bbb9a568eb4c/upload/RESUME-2026-09-08.md`. Do not rely on scratch survival.

Old paths `/workspace/scratch/1f8942b945b3/IceSMP-ww03`, `IceSMP-ww04` and `IceSMP-ww-resume` are not this task's active checkout. Shared Git/worktree caches may contain other sessions; never clean/reset those to recover this branch. GitHub is durable source authority.

Optional cached tools: `/workspace/world-weaver-dependencies/jdk21/bin/java` and javac; `/workspace/world-weaver-dependencies/compile/*.jar` (49 actual jars); `/workspace/world-weaver-dependencies/gradle-9.4.1/bin/gradle`. Current local preflight output is `build/resume-preflight/classes`, all-source list `build/resume-preflight/sources.args`; runtime resources must include `src/main/resources`. Paths/process state may disappear. Do not infer a fixed /opt toolchain or kill a shared Gradle process.

Required verification, with relevant targeted native/provider tests first and then the documented full gates:

~~~bash
./gradlew trashNativeInspectionRegressionTest trashWeaverInspectionRegressionTest trashHistoryRegressionTest trashAnomalyRegressionTest trashArchaeologyRegressionTest trashRuleFieldRegressionTest trashRelicRegressionTest trashProductionHardeningRegressionTest --console=plain --no-daemon
./gradlew build --console=plain --no-daemon
python3 scripts/check_consistency.py
python3 scripts/test_player_profile_authority.py
python3 scripts/audit_world_weaver_coverage.py
python3 -m unittest scripts.tests.test_world_weaver_architecture
~~~

Run coverage --release for final closure only: it is expected to reject current blockers. Update hashes/public-method/bootstrap inventories only after reviewing actual changes. Preserve source guards' invariants when native APIs change; behavioral tests must prove replacement semantics. Check resource pack, architecture, inventory and mirror requirements; hidden WorldWeaver documents are excluded from public gameplay guides and IceSMPGuides. No public permissions/admin guide/HUD exposure.

Commit/push remains authorized; no merge/release. Direct git lacks write credentials in this environment; the connected GitHub API successfully published the source commits, including `60da0df` and `7375034`. Discover exact callable schemas through ALL_TOOLS. Tool results expose structuredContent directly; github_fetch holds JSON in structuredContent.content. Read decoded job logs rather than declaring runtime PASS from a job title. The final documentation-only head may start new workflows; do not attribute source-head results to a different SHA without checking. The final PR metadata and response identify that documentation commit separately.

API publication: stage only reviewed own files; compile and consistency-check; git write-tree. Create UTF-8 blobs and verify exact local blob SHAs; create tree against actual remote parent tree and require equality with staged tree. Recheck PR head, create commit, update_ref with force=false. If a newer head appears, fetch/review it first. No forced branch rewrite. Local fetch may need an explicit remote tracking refspec; a branch-only fetch initially populated FETCH_HEAD but no origin ref. After fetching the exact published commit and proving both staged/worktree contents equal it, reset-soft/fast-forward the active worktree only. Never reset a newer remote or unrelated dirty worktree.

Retain commit trailers:

~~~text
Co-Authored-By: OpenAI Codex <codex@openai.com>
Claude-Session: codex-work-world-weaver-2026-09-08
~~~

## Fresh-agent execution order and acceptance status

1. Read this primary handoff and the specified current instructions, full normative design and supporting evidence.
2. Verify GitHub HEAD/branch/PR/stack/workflows and all newer cumulative changes. If an actual new CI failure exists, investigate its exact logs first. Never reintroduce the old removed-field probe or restart completed native inspection.
3. Implement the remaining native projectile-wall durable consume/remove receipt/recovery and cross-region lifecycle obligations at the existing authority, with actual native integration and relevant failure tests. Preserve the published co-owned safety restriction until the replacement is proved. No developer wall action before handoff/lifecycle/influence closure. The active instruction update above now requires resolving the resumed lifecycle findings and any new CI failures before this WW implementation step.
4. Extract shared canonical anomaly/relic activation and implement the still-missing complete Trash actions, durable journal/recovery/prototype/influence work. Preserve native nonblocking read ports and read-only subset honesty.
5. Continue all remaining design phases and WW-04/WW-09 integrity/coverage closure. Current scope remains full WW-00–WW-09, not this one checkpoint.
6. Perform targeted and full required gates, publish all completed source work, check exact HEAD CI and actual runtime logs. Never merge automatically.
7. If full implementation is still incomplete, update this primary resume with exact new source/doc heads and evidence limits, then provide the four separate categories below.

Implemented in this continuation: native safe co-owned wall admission without frozen foreign-owner handoff; bounded tracking lifecycle and owner-correct native field/reservation cleanup; history/anomaly uncertain-write mutation/save fences; immediate busy/reentrant wall-transaction refusal and admission recheck after actual compaction; real failure/concurrency/WAL regressions; exact two upstream temporary-Git-fixture maintenance fixes. Earlier smoke, inspection and Territory work remains implemented; do not redo it.

Truly unimplemented: full cross-region wall consumption/removal receipt and recovery, after-acknowledgement/after-admission crash and shutdown closure, complete native influence admission; shared anomaly/relic activation APIs; all seven developer Trash actions and durable mutation/recovery/prototype/influence paths; broader WW-04 universal damage/reward ingress closure; remaining provider gaps; WW-06–WW-09 full scope and coverage closure. The active instruction update records the now-implemented lifecycle source repair and remaining client/deployed-artifact evidence gates; it also supersedes the historical unported ROADMAP note.

Implemented but evidence-required: connected-player slot/GUI/client inspection, real co-owned wall consumption/interception and failure/quit/unload observations, populated/cross-region interactions of implemented territory/PvE paths, multiplayer/human playtest and all documented client/resource acceptance. Empty startup/shutdown and detached-item proof does not satisfy these gates. Missing cross-region wall receipt/recovery is implementation work, not merely an evidence request.

Remaining blockers: 51 WW implementation blockers, explicit native wall receipt/lifecycle/influence gaps, unfinished universal reward/influence closure, required actual environment evidence, and the reported dev server's 18 missing/mismatched authored NPCs. Equipment audit/reload and deferred HUD/disable findings are not claimed resolved. No world coordinates, builder bindings, deployed JAR state or connected test results were invented. No full-domain completion, zero-leakage, merge-ready or production-ready verdict. Production coordinator and artifact gates remain disabled.
