# IceSMP WorldWeaver — resume point, 2026-09-08

Internal developer handoff. This is a continuation checkpoint, not a completion or release report.

## Start here

Continue the full Világszövő / WorldWeaver implementation for `MilCsik09/IceSMP`. The normative specification is `docs/development/world-weaver/design-v2.md`; read it in full before implementing. The original user requested the complete WW-00–WW-09 scope, reviewable stacked phase PRs, dynamic providers/content discovery, zero unresolved coverage blockers, Folia safety, durable recovery and zero SANDBOX reward leakage. Do not cut this to an MVP, admin wand, command registry or scripting system.

**Current continuation branch: `feature/world-weaver-ww05-trash-territory`, PR #159.** Last source-changing head: `aeb74df060029e63595ce27c03d25946cebe0442`, tree `e5b90de4b45048fb878983b62799b800c33976a2`. This handoff is intended to be committed directly above that source head. Fetch the branch and use its actual remote head, which may include this documentation commit or subsequent work. Do not reset a newer head to the hash above.

First implementation task: fix the confirmed Trash Paper/Folia smoke-probe integration regression described below. Then continue the incomplete Trash provider and the rest of the specification. Production execution is intentionally disabled. No merge, release or production enable has been authorized.

## Verified remote stack

The following refs were re-read from GitHub on 2026-09-08 before saving this handoff. Every PR was open, draft and unmerged. Phase existence does not imply phase acceptance.

| Phase | PR | Branch | Head before this handoff | Evidence/status |
| --- | --- | --- | --- | --- |
| WW-00 | #153 | `feature/world-weaver-ww00-coverage` | `0a3cd260260042e91d79b638510239cc18e2c59c` | Inventory established; final coverage closure still blocked |
| WW-01 | #154 | `feature/world-weaver-ww01-artifact` | `178e186a6421df98549a3915c4b33bba924a4678` | Existing DEV lifecycle generalized; client/lifecycle evidence still required |
| WW-02 | #156 | `feature/world-weaver-ww02-kernel` | `d57fff9f53ab57b3ce557d20a24c2821d891b3c5` | Generic kernel/frontend vertical slice and regressions |
| WW-03 | #157 | `feature/world-weaver-ww03-durable` | `17d985dc3a9d8fe62e11ee907efced823bc67689` | Journal/projection/recovery/Undo foundations; later controls remain |
| WW-04 | #158 | `feature/world-weaver-ww04-pve-faction-integrity` | `ac6e46eb96a6d4cd8b00e778afb775acdeb2ae4c` | Substantial implementation; universal influence/reward ingress and provider gaps remain |
| WW-05 | #159 | `feature/world-weaver-ww05-trash-territory` | `aeb74df060029e63595ce27c03d25946cebe0442` | Territory surfaces + native Trash field seam; phase incomplete; smoke CI failing |
| WW-06–WW-09 | — | Not created in this work | — | Required implementation and closure remain |
| WW-10 | — | Not created in this work | — | Production enable/merge is a separate decision |

Stack dependency: #155 → #153 → #154 → #156 → #157 → #158 → #159.

Cumulative gameplay base is #155, `feature/faction-crime-whisperer-rework`, commit `2a78eb6071db063740ce1f8e6889b03d9741dbb0`, tree `6389054ac358fdddb066c665dcee9d6acab4679e`. It builds on the originally specified #152, `feature/trash-production-hardening`, commit `a335b3b5acaea66772534e51527a1c9233a85d1a`. The WW stack was refreshed bottom-up without rewriting that cumulative history. Do not start from master. Re-check current open gameplay PRs before creating another phase branch.

WW-05 exact phase base is WW-04 `ac6e46eb96a6d4cd8b00e778afb775acdeb2ae4c`, tree `373e4c9769097e3679397dbcf43287133ec6b59a`.

## Immediate blocker: actual failing CI, not a hypothetical concern

Source commit `aeb74df060029e63595ce27c03d25946cebe0442` extracted native field ownership from private `TrashRelicRuntime.fields` and `claimedFields` into the single `TrashRuleFieldService`.

The inherited `src/main/java/hu/taliann/icesmp/trash/TrashProductionRuntimeProbe.java` still calls `readField(relic, "fields", Object.class)` at startup and includes `fields`/`claimedFields` in reflective shutdown inspection. Those members no longer exist. Both runtime probes fail with `IllegalStateException`, followed by `MissingStartupProof` on shutdown. The probe deliberately hides exception messages to prevent hidden identity leakage.

Fix the probe to use the actual native service snapshot. Preserve startup empty-field/empty-claim evidence and shutdown empty-field/empty-claim evidence; explicitly verify service open at startup and closed after shutdown. Do not skip, delete or weaken the Paper/Folia marker checks. Prefer typed native diagnostic access over reflection/private-map inspection; do not introduce a fake service or restore duplicate field registries just to satisfy the probe.

Current source-head CI, rechecked 2026-09-08:

| Workflow | Run | Result |
| --- | --- | --- |
| WorldWeaver verification | [34156749483](https://github.com/MilCsik09/IceSMP/actions/runs/34156749483) | SUCCESS |
| Resource pack validate-only | [34156749490](https://github.com/MilCsik09/IceSMP/actions/runs/34156749490) | SUCCESS |
| Repository Docs Inventory | [34156749654](https://github.com/MilCsik09/IceSMP/actions/runs/34156749654) | SUCCESS |
| Trash production hardening | [34156749485](https://github.com/MilCsik09/IceSMP/actions/runs/34156749485) | FAILURE |

Trash job details: Java 21 automated gates `101850057527` succeeded; Folia job `101850057710` and Paper job `101850057712` failed the proof-marker/clean-shutdown requirement. This is a blocking CI integration finding. No fix was implemented before this handoff. Do not describe the whole source head as CI-green.

## What the last source commit changed

Ten files changed at `aeb74df`:

- `src/main/java/hu/taliann/icesmp/trash/TrashRuleFieldService.java` — new canonical transient field authority.
- `src/main/java/hu/taliann/icesmp/trash/TrashRelicRuntime.java` — all native field admission, geometry, claims, removal, expiry and shutdown delegate to the one service; exposes `ruleFields()`.
- `src/regression/java/hu/taliann/icesmp/trash/TrashRuleFieldRegressionSuite.java` — actual bounded/concurrent state tests.
- `TrashRelicRegressionSuite.java` and `TrashProductionHardeningRegressionSuite.java` in the same regression package — existing native wiring assertions follow the extracted service.
- `build.gradle.kts` — new suite registered as a check dependency.
- `scripts/player_profile_authority_allowlist.json` — the transient field-ID map is explicitly RUNTIME.
- `docs/development/world-weaver/coverage.json` — reviewed native service/source inventory; no domain promoted to FULL_PROVIDER.
- `docs/development/world-weaver/WW-05-territory-policy.md` — exact evidence and remaining work.
- `docs/ARCHITECTURE.md` — existing package file count only, 41 → 42; no new public developer feature documentation.

The service is Bukkit-free. Immutable points contain world UUID plus finite coordinates; fields retain no Location/World object. Four native kinds: PROJECTILE_WALL, ACOUSTIC_NULL, CEASEFIRE, SPATIAL_ANCHOR. Caps remain 32 per world / 128 globally, with radius ≤32 and lifetime ≤60,000 ms. Native placement defaults fit these caps.

`removeIdle` requires an exact service revision and immutable field match and rejects active native claims. Expired claims cannot authorize late inventory consumption. Claimed expired reservations remain until the continuation releases them, then expiry returns them for native owner cleanup. Shutdown drains fields and closes admission. Native brick reservations/history are not forged. No Weaver field prototype or persistent field Fork was implemented by this extraction. Acoustic events use their event location instead of reading another entity's location.

Local evidence recorded before the environment disconnected: full main/regression Java 21 compile, zero errors and three inherited warnings; TrashRuleField **182 assertions**; existing TrashRelic and TrashProductionHardening suites pass; architecture **4 tests**; consistency **0 FAIL / 0 WARN**; profile authority **689 classified / 0 unknown, stale, invalid or transition entries**. Inventory: **338 authorities, 55 domains, 51 implementation blockers, 0 audit errors**; **1250 main Java files**. The 182 assertions include 32-way claim contention and 32-way admission at capacity. These checks do not replace the failing live smoke gates.

## WW-05 Territory checkpoints already published

Detailed design mapping and checkpoint-specific limitations are in `WW-05-territory-policy.md`. Earlier paragraphs are chronological; later checkpoints supersede only the specific implemented gaps.

| Checkpoint | Commit | Principal evidence |
| --- | --- | --- |
| Shared native protection policy | `8be262e05a29f8f26d53416fe62fafa43b580825` | 419 policy assertions; true native handlers and regeneration precedence |
| Native canonical transaction | `5fb86b1ddb1ecfb15fbb5aa1950b3302831ad4b0` | 83 real-YAML assertions, atomic receipt/state publication, fault injection |
| Bound projection provider | `85e6c45f232e7980799e60ae6f11d8492d47ac8e` | 86 assertions, dynamic catalogs, Thread, projection consumer and crash boundaries |
| Canonical provider adapter | `3af3bd0816793dd06ac22a2bd98684d411c72a13` | 94 assertions, four LIVE_GM actions, durable-before-I/O, observed recovery and Undo |
| Local Subject inspection | `be23de1332814b91e97c4b33cdf2b116ac71754d` | 59 assertions, PLAYER/ENTITY/BLOCK/LOCATION detached trace capture |

At `be23de1`, all four workflows succeeded: WorldWeaver `34154553019`, resource pack `34154552965`, Trash `34154552954`, docs `34154552893`. This is the last fully green pre-field-extraction checkpoint; do not roll back the native service to hide the probe mismatch.

TerritoryManager now stages immutable zones/spawns/chunk index/native receipts, atomically writes YAML, then publishes after acknowledgement. Uncertain writes retain the acknowledged live snapshot and fence writes until load assessment. Polygon geometry is defensively copied. Native `adjustConditionally` supports rename/type/owner/Y-bounds with expected SHA-256 fingerprints, exact operation/request/native receipts, replay acknowledgement without replaying mutation, explicit capital collision and a bounded 4096-receipt ledger. Undo creates a new compensating native history entry. Provider I/O runs on AsyncIoOwner after PREPARED with final authority/world fingerprint admission.

TerritoryWeaverProvider registers via the existing composition-root provider factory. WORLD actions include projection, sever/clear, conditional Undo, typed rule Thread and four canonical actions. Native enums/zones back dynamic catalogs; no core/artifact/GUI domain branching was added. The native protection service consumes projected rules through circuit isolation. Canonical claims, ownership, treasury/tax/history and reward identity do not consume projections. ALLOW, including sever that reveals an older ALLOW, requires LIVE_GM. SANDBOX world influence is published atomically with projection and is not washed by sever, Undo or restart.

Local PLAYER/ENTITY/BLOCK/LOCATION facets inspect typed native zone metadata and ordered decision traces. Live handles remain in owner callbacks; coordinate subjects require already loaded chunks; drift is rejected. Context is explicit player-actor or environmental, with no invented PvP victim. WORLD mutation actions are not exposed against local subjects. Generic local-to-world navigation remains WW-07 work. Populated/cross-region protection, world lifecycle and client evidence remain required.

## Foundation already present — preserve and extend

Source root is `src/main/java/hu/taliann/icesmp/dev/weaver/`, not `.../worldweaver/`. Runtime authority adapters also touch native subsystem packages.

- Existing DevItemManager/DevItemFactory were generalized for the physical developer artifact; no second DEV lifecycle framework. Durable singleton identity, duplicate/forgery checks, recovery and four modern resource-pack states exist. Bingulus remains separate.
- Primary-developer UUID is `2d47d7b6-294e-4a14-922c-befacd66ee6d`; HiddenDevAuthority is the boundary. OP, admin and permission checks cannot replace it.
- Generic seven SubjectRef kinds; typed, namespaced/schema-versioned YAML-safe values; provider/coverage/type/catalog contracts; descriptor-only GUI, dialogs, confirmation, history/diagnostics, Thread Case/Recent/Quick Apply; five bounded AREA geometries and continuation-based owner routing.
- Durable PREPARED/APPLIED/COMMITTED protocol, atomic receipt/projection/influence publication, ABORTED/COMPENSATED/NEEDS_REVIEW states, observed recovery, unavailable-entity pending/load wakeup without force-loading, scoped revisions and conditional Undo. Entity/item/event influence is monotonic; player/world/spatial tail is retained. Unknown providers/content remain opaque and fail closed.
- Recovery of a caught ambiguous entered-stage failure remains NEEDS_REVIEW; automatic recovery targets PREPARED/APPLIED. Manual developer review controls are not finished. Never force-retry or manufacture a receipt.
- Provider circuit isolation, bounded queues/operations/receipts/areas, retirement/shutdown handling and journal retention pins exist. Dynamic fixture tests demonstrate registry content and dummy-provider discovery. That does not establish universal subsystem completion.

Current composition registers MinecraftWeaverProvider, PvEWeaverProvider, FactionWeaverProvider and TerritoryWeaverProvider. **There is no completed TrashWeaverProvider at this head.** Reward-gate adapters for a domain are not the same thing as its generic developer provider.

Production execution remains disabled: the WorldWeaverRuntime composition passes `() -> false` to the execution coordinator. Retain the existing artifact/production gates until separately authorized.

## WW-04 implementation and unresolved integrity work

Read all relevant `WW-04-*.md` documents in this directory before changing these paths. They contain exact source/checkpoint/test evidence, not universal PASS verdicts.

Already present:

- PvE registry-backed ability/template/rank/archetype catalogs, typed Threads, session/persistent projections and the actual native projection port; canonical loot/Bestiary identity retained. Force ability uses the real cast lifecycle and validated target/cooldown/cast epoch. Runtime control ledger and bounded recovery exist.
- Faction temporary membership/entity semantic contexts use canonical passive/target policy; CROWN_CURSE remains native. Canonical membership uses native conditional profile WAL, real receipts and pending cleanup before benefits. Native guild/council/king/raid cleanup ordering and offline/online recovery are guarded.
- Neutral reward eligibility and effect/source bindings are exclusive and fail closed. Several kill/drop/XP and class/pet/Bestiary/Quest/community/currency beneficiaries are gated; earned WAL/outbox replay remains valid.
- GameplayEffectContext carries bounded original sources/targets. Durable source/target influence precedes a single-use owner-revalidated effect permit; actual lifetime observers retain potion/other influence until observed end plus quarantine tail. Provider failure/unavailability cannot prematurely clear influence.
- Known native parent/source UUID propagation precedes unknown child creation; actual authored/summon origin APIs and causal source capture are bound. Native PvE arrows/summons stay inert until permitted. Owner callbacks and immutable causal context replace several foreign entity reads.
- Server challenges use real run UUIDs and original lineage. Community/season credit has native final gates and real earned outbox handling. Quest progress/completion uses canonical WAL and receipt paths; queue lanes bound and serialize mutations. Statistics/profession/knowledge earned transitions have additional final gates and regressions.

Still required; do not declare zero reward leakage:

- Comprehensive native damage/projectile/DOT, player/item/event/spatial/world influence propagation, including crossing into/out of world/spatial quarantine.
- Remaining shared season/raid/war/duel/spy/bounty/crate/personal-loot/parkour/currency-faucet routes and their original causal sources/final gates.
- PvE stats/health/resistances/source-combat contexts, AREA/cross-region capabilities; Faction scripted target/peace and remaining effective passive surfaces.
- NPC quest hand-in currently removes items before async progress settlement; physical reward outbox and chain/spec/sins/spell/guild followups need closure.
- Hidden discovery first-finder/item outbox, community synchronous completion I/O and ephemeral server-challenge reward outbox remain.
- No general safe synchronous damage admission/replay solution was implemented. Cancel → persist → replay damage loses/doubles native critical/sweep/enchantment/knockback semantics. Do not use fake events or copy unsafe live-handle damage replay. Foreign player UUID alone does not capture world/spatial causal sources. Audit concrete handlers rather than claiming a global shortcut.

Earlier fixture assertions include effect propagation 214, observed influence 157, causal sources 51, influence persistence 162, server challenge 125, profession 74 and knowledge 133 at their documented checkpoints. Do not present these as newly rerun results for this handoff.

## Trash next steps after smoke repair

Design section 33 requires normal runtime and developer adapter to share TrashAnomalyActivationService, TrashRelicActivationService and TrashRuleFieldService. Only the last seam has been extracted at this head.

Required inspect: hidden base/kind/behavior/primitive/lifecycle; instance UUID/revision; provenance/history; archaeology facts; anomaly memory; actual rule fields. Required actions: individualize_unit, activate_behavior, transition_success, repair, give_sandbox_copy, create_rule_field, remove_rule_field. Canonical activation must use the real runtime/history transaction, without synthetic Bukkit events, raw provider PDC writes or fabricated natural-history events. A DEV_PROTOTYPED event, if added, must not count as a natural archaeology fact.

Native sources to inspect:

- TrashHistoryService owns per-item identity markers, history validation, individualization, transform/repair/split and inventory projection.
- TrashHistoryStore owns the actual durable history/journal and native snapshots.
- TrashArchaeologyFactEngine and TrashArchaeologyService derive/award facts; do not call an awarding route merely to inspect.
- TrashAnomalyStateStore owns per-instance memory; TrashAnomalyRuntime owns native anomaly behavior.
- TrashRelicRuntime now owns the one TrashRuleFieldService.

New audit observation before handoff: TrashHistoryStore find/matches and TrashAnomalyStateStore get are synchronized, and their write methods can hold the same monitor during I/O. An owner-thread provider must not blindly call these and claim nonblocking Folia safety. Investigate a native acknowledged nonblocking read seam or detached I/O continuation with fresh owner validation. No lock refactor, new read seam or Trash inspection provider was implemented. A possible design was discussed internally but never selected; do not treat an unimplemented lock strategy as normative.

Known native projectile-wall continuation gap: tracking captures prior velocity, freezes a projectile and schedules the owner-player inventory consumption callback, then returns to the projectile. Existing callbacks still retain Projectile/Vector handles. Disable/unload/cancelled handoff can leave a frozen projectile or an unresolved reservation unless hardened. The field extraction fixes detached field state and expired-claim admission only. Do not expose developer wall creation before repairing and testing handoff/lifecycle/influence. Do not forge inventory reservation tokens for prototypes.

## Remaining phases and acceptance

- WW-06: actual Itemization/Event/DeveloperSelf/Knowledge providers through safe native APIs; explicit arming, vanish via VanishManager, artifact recovery/session cleanup and manual NEEDS_REVIEW/unknown-content controls.
- WW-07: multi-field Imprint/Impress whitelist, full Thread/AREA selection UX and generic subject navigation. Do not imprint UUID, owner, event/reward/economy receipts, immutable history or profile revision.
- WW-08: bounded SANDBOX-only Binding (TTL/uses/cooldown/lineage/loop/caps) and provider-owned Fork prototypes for PvE, Trash rule fields and territory overlays; no arbitrary clone, event lifecycle fork or scripting.
- WW-09: close all current-domain coverage, including class/spec/talents/resources/cooldowns, profession/economy/pets/moderation/challenges/current Doom and anything newer in the cumulative repository. Biome was absent at the audit, not an excuse to miss a newly added subsystem.
- Final acceptance: coverage blockers 0, complete design → implementation evidence audit, required regression/resource CI, real Paper/Folia and explicit connected-client/human evidence gates, no known C0/C1 finding, pushed merge-ready stack. No automatic production enable/merge.

Risk, integrity mode and lifetime are separate axes. Projections require concrete effective consumers. Canonical mutation must remain in each subsystem's native authority or explicit projection consumer. Undo refuses external drift. No force undo, reflection debugger, raw YAML/PDC mutation, fake events, receipt spoofing or legacy scheduler. No permanent HUD or public player/admin/permission documentation for WorldWeaver or hidden Trash surfaces.

## Recovered local work and archival checkpoints

The previous environment disconnected on 2026-09-07 after the active WW-05 worktree had been reset-soft to the published source commit and verified clean. No source edits were made afterward. On 2026-09-08, the filesystem became accessible but had restored older WW-03 and WW-04 worktrees and no WW-05 directory. Their modifications differ from the newer cumulative code. To obey the user's request to commit/push all work without overwriting the newer stack, both exact older working trees were preserved on separate checkpoint branches.

| Archive only — do not merge | Remote commit | Exact tree | Captured changed files |
| --- | --- | --- | --- |
| `checkpoint/world-weaver-ww03-local-20260908` | `6cba6e2124839ecb09cb9b07383814f3e45a4569` | `ffa5a100e4fc50e0787eb32edf63ac52bb8b56e7` | 22 |
| `checkpoint/world-weaver-ww04-local-20260908` | `46f51c8ce6df49f8a23ac740c804f3afdf842bf7` | `aaaa44245b2b63420fb4b479b3c891740a087699` | 15 |

WW-03 archive parent is `14ed612f811b03c5aef7a8c615dd35eb618b867a`. It captures the older AREA/kernel execution work and the exact locally modified `dev-assets/trash/source/batch-004.png`. This old image is a preservation artifact, **not a replacement for the repaired current asset pipeline**. WW-04 archive parent is `f1be063fe5664fc7471a94933ab4b22d90ded9f5`; it captures the older observed-influence work. No build/readiness claim is made for either obsolete snapshot. Their existing user indices/worktrees were not reset or cleaned. Ignored build caches/generated outputs are not part of the source checkpoints.

Remote tree hashes were required to match locally constructed Git trees; blob hashes were also checked. Direct git push lacked credentials, so the already connected GitHub API published the commits/refs. These archive branches have no phase PR and are outside the cumulative dependency chain. Continue from #159, never from an archive. If old paths reappear, inspect before editing and do not blanket-reset them.

At handoff the fresh documentation worktree is `/workspace/scratch/1f8942b945b3/IceSMP-ww-resume`, locally named `docs/world-weaver-resume-20260908` and based on the exact source head above. That local name is just the handoff worktree; the published handoff belongs on the WW-05 branch. Scratch paths and process state are not guaranteed to survive a new conversation. GitHub is the durable source.

## Build, test and publishing notes

Read AGENTS.md; use the current full Gradle Java 21 build when dependencies are reachable. The user explicitly authorizes internal developer documentation and stacked cumulative work despite default master/public-doc conventions. This handoff does not change those instructions.

Usual commands from the fresh current checkout:

~~~bash
./gradlew build --console=plain --no-daemon
./gradlew trashRuleFieldRegressionTest trashRelicRegressionTest trashProductionHardeningRegressionTest --console=plain --no-daemon
python3 scripts/check_consistency.py
python3 scripts/test_player_profile_authority.py
python3 scripts/audit_world_weaver_coverage.py
python3 -m unittest scripts.tests.test_world_weaver_architecture
~~~

Run `audit_world_weaver_coverage.py --release` only when evaluating final closure; it is expected to reject the current 51 blockers. Update authority hashes/public method inventory only after actually reviewing source changes. Do not clear blockers by renaming a partial provider's status.

Current environment has cached JDK/compile libraries under `/workspace/world-weaver-dependencies/`: JDK 21, Gradle 9.4.1 and a compile jar directory. They are optional local preflight tools, not durable artifacts. Earlier Gradle attempts encountered dependency access/shared-cache locks; do not kill another session's build or clear shared caches. Full Java-source javac against cached libraries was used as preflight, followed by real GitHub Gradle/boot workflows. Three inherited warnings are not new errors. Existing new suites are check dependencies.

For Paper/Folia proof, read the actual workflow/job logs and require PASS markers plus clean shutdown. Boot-only workflows do not prove connected interactions, cross-region populations or client model/GUI behavior.

GitHub connector notes: discover exact tools through ALL_TOOLS. Results in this session used `structuredContent` directly; fetch responses contain a JSON string in `structuredContent.content`. `github_fetch` supports approved GET endpoints for PRs, actions/runs/jobs and files. Use `github_fetch_workflow_job_logs({repo_full_name, job_id})` for decoded job logs. Avoid dumping full runtime logs with hidden identity details. On API publication, create blobs/tree, verify the tree against the local staged tree, re-check the remote parent, create commit and fast-forward ref with force=false. Never overwrite a newer remote head.

Normal implementation commits retain trailers:

~~~text
Co-Authored-By: OpenAI Codex <codex@openai.com>
Claude-Session: codex-work-world-weaver-2026-09-08
~~~

## Handoff-only verification

This document changes no Java, configuration or assets. On the fresh source-head worktree, consistency passed with 0 FAIL / 0 WARN, the coverage inventory reported 338 authorities / 55 domains / 51 blockers / 0 audit errors, and all four architecture tests passed. A new full Java build was not run for this Markdown-only checkpoint; the source head's real WorldWeaver CI compile evidence and the failed Trash runtime evidence remain exactly as listed above. Archival snapshots carry no compile/CI acceptance claim.

## Fresh-agent execution order

1. Read this handoff, AGENTS.md and the full normative design; inspect the internal WW-00–WW-05 evidence documents and coverage matrix.
2. Query current PRs, fetch the actual #159 head and use a clean worktree. Preserve any unrelated/current-session dirty state. Verify no newer cumulative gameplay work must be accounted for.
3. Fix the concrete Trash probe/service mismatch; retain all lifecycle proof and secrecy constraints. Compile, run targeted regressions/consistency, commit/push to WW-05 and require Paper and Folia Trash CI green.
4. Continue Trash native activation/history/inspection/prototype/rule-field integration, fixing discovered authority/thread/reward/recovery problems at their source.
5. Close remaining WW-04 integrity gaps, then build the required WW-06–WW-09 stacked phases. Keep generic artifact/core/GUI unchanged for domain content/provider additions.
6. Maintain exact source/test/CI evidence and the coverage matrix. Produce the originally requested exhaustive final report only when there is evidence. Never infer full completion from a green build or the existence of this resume point.

Resume verdict: **implementation incomplete; 51 coverage blockers; confirmed Trash smoke regression; stack not merge-ready or production-ready. All source changes known to this handoff are either in the cumulative stack or the explicitly separate archival checkpoints.**
