## Current continuation — intrinsic prototype identity and native quarantine (2026-09-09)

The full implementation mandate remains active; this is not a task-completion or provider-closure claim. Source parent is `480c5146ba55b1f6a7f6446cc6a195250d7346dc`, tree `b5b4caf39d9c2e62f1b926de0dcc030cc7289c67`, on the same feature/#159/base #158. All four parent workflows succeed: WW `34394645568`, Trash `34394645526`, resources `34394645404`, Docs `34394645560`. Decoded build job `102611209656` confirms full build in 1m51s plus a 13s evidence invocation, including creation452/provider298/causal56. Actual native Trash jobs `102611209693`, `102611210047`, `102611210052`, `102611210149` pass; cooperative probes have zero connected players. Docs job `102611209221` confirms 50 tests/655.732s, zero FAIL/WARN consistency and zero reported blocking inventory findings. Parent evidence does not prove this successor.

Native `ItemMutationService.clonePrototype` produces a detached candidate with a fresh item UUID, explicit `DEV_PROTOTYPE` state, original-source reference in a separate `DEV_PROTOTYPED` history event, and exact developer/operation provenance. It copies actual authored roll/rune/ascension state but does not copy natural crafting/masterwork/history claims. This pure builder grants no issuance authority and is not yet a WorldWeaver item mutation action. Native codecs, reroll, ascension and rune transitions preserve intrinsic prototype identity. `ItemIdentityService` regenerates and verifies `dev_prototype` custody metadata from that identity and keeps attributes, enchants, ability power, rune/set/signature contribution suppressed through render/reconciliation. Removing marker PDC cannot turn an intrinsic prototype into a normal managed item. The existing `dev_item_id` artifact remains distinct.

Shared `ItemPrototypePolicy` recognizes direct and bounded nested bundle/container/charged-crossbow custody, including malformed markers. An unreadable nesting limit refuses a transformation without deleting an unidentified ordinary item. Only a singleton matching the immutable HiddenDevAuthority primary developer (not a configurable artifact owner or OP) and owner metadata remains in personal inventory/cursor. Native DEV owner refresh, join/respawn, hand/inventory activity, ender chest, container views, entity reload and bounded 32-container chunk callbacks perform owner-local quarantine. Physical movement, drop/pickup/hopper, bundle/creative, use/place/consume/launch/attack/harvest and craft/smith/crafter boundaries are guarded. The existing native transformation policy denies every prototype transformation route. Market, Trash vendor, salvage, normal item mutations/material payment, equipment authority, signature/rune consumers, native Trash behavior and archaeology progression separately refuse prototypes. Hidden Trash inspection includes the native prototype marker and valid custody identity.

Final local verification: all 1446 main/regression Java 21 sources compile against 49 actual CI dependency jars with zero errors and three inherited warnings. Itemization140, vanilla-boundary125, equipment284, rune38, creation452, handoff575, receipts124, native-inspection97 and provider298 assertions pass, plus relic/production hardening. Profile guard: 703 classified, zero unknown/stale/invalid/transition; four architecture tests; consistency/mirror zero FAIL/WARN. IceSMPGuides draft #20 is updated at `d18a150f3fd4a0a00fedc2db8a4a7b4cb61db0a9`. The successor native Paper/Folia probe tests actual detached ItemStacks, serialization, nested containers, marker loss, canonical render suppression and real transformation classification; only its eventual current-source CI logs constitute execution evidence. No connected-player custody, Minecraft playerdata crash recovery or prototype issuance is claimed.

Coverage now classifies 341 authorities / 55 domains / 51 whole-domain implementation blockers. The new native policy belongs to itemization; its physical adapter is assigned to itemization/Trash/developer-self without changing any full-provider status. The only public documentation change is the verified listeners package count (123 to 124), mirrored byte-for-byte. Listener wiring is below command registrations, so registration locations are unchanged; only reviewed core/probe source digests change. The previously recorded strict inventory component findings remain unresolved and are distinct from report-mode CI success.

Continue all seven native Trash developer actions and their existing-WAL receipts/conditional recovery, complete shared Anomaly/Relic activation and the WorldWeaver item mutation ingress using the existing `ItemMutationCoordinator`/journal/render path. Do not issue a prototype through the detached builder directly or add a second inventory/history ledger. Remaining WW04 ingress and full WW06–WW09 scope stay required. Missing authored NPC bindings remain NONFATAL, required FancyNpcs plugin/API startup separate. Feature/draft stack publication is authorized; no merge, release or production enable.

## Previous continuation — shared native rule creation and consequence lineage (2026-09-09)

The owner now explicitly requires completion of the full WW implementation without stopping at individual checkpoints. This checkpoint is ongoing implementation, not closure. Source parent: `185bcacea08be7347fb42cedbdbe517f7c856ef6`, tree `3837a391434560e0b23bd7f4a3ae38cd999a9e31`; existing feature/#159 and base #158 `ac6e46eb96a6d4cd8b00e778afb775acdeb2ae4c` remain unchanged. Resolve the public successor HEAD before resuming. All four parent workflows pass: WorldWeaver `34389427664`, Trash `34389428348`, resource pack `34389427478`, Docs `34389427651`. Decoded parent build job `102593791484`, native Trash jobs `102593794759`, `102593794823`, `102593794886`, `102593794948`, and Docs job `102593791888` confirm their actual evidence. Cooperative probes have zero connected players; parent evidence is not successor evidence.

All four native rule kinds now share `TrashRelicActivationService` inactive reservation, owner dispatch, two independent journal permits, final native history admission and acknowledged field publication. The three success-transforming fields use the real selected hand, planned native singleton UUID, authored phase and original natural origin in the existing history WAL. An exact `CreationWrite` capability retains existing capacity while an entered acknowledgement outlives deadline/owner cancellation. No replacement field/history authority, executor, synthetic event or refund is added. Named creation ports retain UUID/serialized item data/scheduler handles and resolve current ownership before live reads.

Combat and acoustic event consequences use native field identities and actual affected entity/location sources. Pending provenance writes never retain or replay a Bukkit event: only an immediately acknowledged permit may modify that event; a later native event captures afresh. Trash contributes each actual active overlapping field Event through generic causal-source discovery. Preparations, expired fields, other worlds and geometry misses are excluded. Two existing source-gated PvE knockbacks explicitly use the native spatial-anchor policy. This does not claim complete remaining Relic/Anomaly influence ingress.

Source budgets remain explicit and frozen at registry publication: existing providers default to 16; Trash advertises its actual native 32/world bound; the combined extension budget is 48, leaving room for native causes within the unchanged 64-source context. Nothing is silently truncated. Tests cover 32 actual overlapping fields, invalid/default/frozen provider budgets and exact combined-budget acceptance/refusal. The reviewed core composition addition shifts 68 registration evidence locations by one line; exact command/permission identities, aliases, counts and all gates remain unchanged.

Local evidence: all 1444 main/regression Java 21 sources compile with zero errors and three inherited warnings against 49 actual CI dependencies. Native creation/consequence suite 452, owner handoff 575, receipts 124, native inspection 97, fields 294, provider inspection 298, generic causal capture 56 and influence propagation 227 assertions pass, plus relic and production hardening. The new native runtime probe verifies planned success transitions with real detached ItemStacks/history WAL; its actual Paper/Folia execution still requires successor CI logs. Local Gradle 9.4.1 offline build cannot resolve the uncached run-paper 3.0.2 plugin; it is not a full-build pass. Consistency/public mirror: zero FAIL/WARN; four architecture tests; coverage 340 authorities / 55 domains / 51 whole-domain blockers / zero audit errors. Strict documentation inventory's 249 component findings remain open, distinct from report-mode CI success.

Continue the complete native activation APIs, all seven Trash developer actions, prototype quarantine and native durable mutation/recovery, then remaining WW04 and WW06–WW09 requirements. Connected owner regions/playerdata custody, actual builder/world/NPC data and multiplayer/client/human acceptance remain unproven. Missing authored NPC bindings remain NONFATAL; required FancyNpcs plugin/API startup is separate. Authorized: feature commit/push and existing draft stack. No merge, release or production enable.

# WW-05 Territory native policy seam — internal developer evidence

Current implementation checkpoint is **Native wall interception influence admission** at the end of this document; the primary continuation authority remains `RESUME-2026-09-08.md`. Earlier checkpoint limits are chronological, not instructions to recreate superseded paths.

Branch: `feature/world-weaver-ww05-trash-territory`. Exact immediate base: `feature/world-weaver-ww04-pve-faction-integrity`, `ac6e46eb96a6d4cd8b00e778afb775acdeb2ae4c`. Cumulative gameplay base: #155 `feature/faction-crime-whisperer-rework`, `2a78eb6071db063740ce1f8e6889b03d9741dbb0`. Earlier phases and their audit fixes are preserved; production remains disabled.

## Implemented native seam

`TerritoryProtectionPolicy` evaluates detached facts and produces an immutable decision and ordered trace. `TerritoryProtectionService` uses it for actual build/interact, player/unattributed combat, ownerless terrain, explosion and fire decisions. Its inspection methods use the same evaluator. The read-only, bind-once `TerritoryRuleProjectionSource` port does not mutate territory storage/index, faction ownership or claims.

Hard decision precedence is explicit: DOOM entry grace, combat-tag vulnerability, applicable canonical admin/builder bypass, and native PvP raid-participant override precede rule projections. Projection availability failures are denied after these hard decisions. Foreign actor permissions are not read: uncertain permission capture denies ambiguous protected actions instead of inventing a bypass decision. Native notices resolve the player UUID on its owner callback and retain no Player handle in the continuation.

The previous `denyPvp` DOOM-grace route had no caller. Native damage and harmful splash/cloud consumers now pass victim lifecycle state through `denyPlayerDamage`; an attacker in the DOOM zone forfeits its own grace. Foreign non-player damage sources and unavailable potion recipients fail closed before live reads. This is conservative owner-unavailable behaviour; populated cross-region tests remain required.

An explicit projection denial or unavailable authority cannot be bypassed by temporary terrain destruction/regeneration: the shared decision exposes that constraint to block-break, explosion and entity-change-block paths. Canonical regeneration eligibility remains native when no explicit/uncertain denial applies. Explosion blocks and entity-change-block accesses also guard owner availability before live state reads.

## Verification and exact limits

Java 21 full main/regression compilation passes (three inherited deprecation warnings). `TerritoryProtectionPolicyRegressionSuite` passes 419 assertions across the canonical matrix, hard lifecycle precedence for every overlay/availability combination, foreign-owner uncertainty, provider failure, regeneration denial and immutable trace. Existing TerritoryCapital, FactionPassive and FactionPassiveHardening suites pass. The new suite is part of the Gradle check dependency set.

This checkpoint supplies the shared native consumer seam. It does not yet supply TerritoryWeaverProvider, its journal-backed projection adapter, catalogs/Thread, canonical conditional transactions, or the Trash adapter. No domain becomes FULL_PROVIDER; coverage remains 51 blockers. Native live Paper/Folia damage/potion/regen and connected-client inspection evidence are required. Unit/source checks do not close those gates. No production enable, merge or WW-05 completion is claimed.

Additional local gates: coverage inventory 327 authorities / 55 domains / 51 blockers / zero audit errors; consistency zero FAIL/WARN; profile guard 685 classified / zero unknown, stale, invalid or transition entries. The world-UUID projection interface is explicitly classified as runtime, not a player-profile mutation.

## Canonical transaction checkpoint

The next WW-05 change builds on exact policy head `8be262e05a29f8f26d53416fe62fafa43b580825` (tree `8fc3ac3abf0d5148c9f4f1ed317a1605e38a84cf`). Its WorldWeaver verification run `34149084596`, resource-pack run `34149084643`, Trash hardening run `34149084595`, and docs inventory run `34149084614` all succeeded. Those runs validate that published policy checkpoint; they do not constitute connected Territory interaction evidence or evidence for the following untested-on-CI commit.

`TerritoryManager.adjustConditionally` now owns bounded native rename/type/owner/Y-band transactions. Expected canonical fingerprints reject external drift; the real operation UUID, request fingerprint and before/after fingerprints are persisted in the same atomic YAML replacement as the zone. Exact retries return the existing acknowledgement without replaying the mutation, including after later unrelated native changes. A capital collision is rejected explicitly, without silently modifying a second territory. Compensation uses a new operation and preserves earlier history.

All native territory mutations prepare a detached immutable zone/spawn/chunk-index snapshot, persist it, then publish it through one volatile reference. Failed or uncertain writes retain the last acknowledged live snapshot and fence further writes until disk reload assessment. Failure after actual replacement is recovered from the persisted native receipt; failure before replacement retains the prior disk state. Polygon constructor/accessor copies close the mutable-array alias that could otherwise alter geometry without a canonical transaction.

The native ledger caps at 4096 entries and refuses further mutations at capacity without eviction. Startup rejects malformed, coercible, noncanonical-UUID or contradictory receipts and oversized ledgers. The provider must execute this storage API on its I/O executor after durable PREPARED; this native seam does not itself grant developer authority or implement that provider. Legacy command scheduling is not claimed to be converted by this change.

Verification: full Java 21 main/regression compile; native real-YAML `TerritoryCanonicalMutationRegressionSuite` **83 assertions**, including before/after-replacement failures, immediate pre-write admission denial, eight concurrent writers, immutable publication, receipt restart/replay, compensation conflict, malformed ledger and capacity. Registered in Gradle `check`. Existing TerritoryProtectionPolicy (419), TerritoryCapital, FactionPassive, FactionPassiveHardening and FactionRework suites passed in this worktree before the final strict receipt checks. Profile authority guard: 686 classified, zero unknown/stale/invalid/transition findings. Coverage inventory: 332 authorities, 55 domains, 51 blockers; no FULL_PROVIDER claim. The current source set is 1244 main Java files.

Remaining phase work: journal-backed Territory provider/consumer binding, dynamic catalogs and Thread, Trash adapter, connected Paper/Folia protection and transaction evidence. The earlier “no canonical conditional transactions” limitation applies to the published first policy checkpoint, superseded only for the native seam by this change. Production remains disabled.

## Bound protection provider checkpoint

Exact base: canonical transaction commit `5fb86b1ddb1ecfb15fbb5aa1950b3302831ad4b0`, tree `7a95e967fe6852f45a543033c1b88b26e1ec12b6`. Its WorldWeaver verification `34150867311`, resource pack `34150867326`, Trash hardening `34150867302` and docs inventory `34150867325` all succeeded. This evidence belongs to the canonical transaction head; the new provider change requires its own CI.

`TerritoryWeaverProvider` now registers through the existing composition-time provider factory. Its descriptor-only frontend contribution supplies WORLD facet/inspection, canonical registry-backed zone and rule catalogs, session/persistent rule projection, explicit sever/clear, conditional Undo, rule Thread export/import and observed recovery. New native zone definitions appear through the same catalog instance. Legacy zone identifiers are represented by deterministic digest catalog keys; native IDs and ownership remain in TerritoryManager. No core/artifact/GUI source changed. Quick Apply fills the typed rule and uses the generic parameter flow to select its canonical target zone. Rule Threads omit zone IDs, canonical fingerprints and history.

`TerritoryRuntimeProjectionSource` is bound to the real `TerritoryProtectionService` port through provider circuit isolation. The journal publishes the effective rule and world influence together; PREPARED alone cannot expose a rule. The latest projection for each world/territory/rule wins. The native protection evaluator still applies hard DOOM/combat/raid/bypass precedence. Canonical territory map/index, ownership, claims, treasury/tax/history and reward identity do not consume overlays. A changed canonical zone fingerprint rejects the effective read; explicit sever removes the overlay without rewriting external native changes.

Design section 34 admission is enforced at preparation and at the effective consumer: SANDBOX can deny/inherit; ALLOW requires LIVE_GM. SANDBOX catalogs and Thread admission exclude ALLOW. Sever/Undo that would reveal a previously shadowed ALLOW also requires LIVE_GM, closing an indirect permissive-rule path. Malformed persisted SANDBOX ALLOW values fail closed. LIVE_GM still requires the existing primary-developer arming boundary in the generic kernel. SANDBOX territory projections conservatively quarantine their WORLD subject; sever, Undo and restart retain influence according to the existing journal policy. This does not claim universal reward ingress closure elsewhere in WW-04.

The new `TerritoryWeaverProjectionRegressionSuite` is registered in Gradle check. It uses the actual native manager, generic registry/Thread draft, durable coordinator, serialized journal, consumer adapter, and explicit owner fixtures. It covers catalog evolution, canonical isolation, world/zone/rule scoping, conditional Undo/drift, SANDBOX/LIVE_GM admission, stale/unavailable owner refusal, and before/after failures at four journal/audit writes with unavailable-world recovery. These fixtures do not constitute populated Folia or client evidence. Full local source set: 1247 main Java files; inventory 335 authorities / 55 domains / 51 blockers; profile guard 688 classified / no unknown, stale, invalid or transition entries.

Remaining WW-05 work includes the canonical transaction provider adapter, location/block decision-trace frontend and the complete Trash surface. Overlay Fork belongs to WW-08. Domain coverage remains DEFERRED_BLOCKER. The provider registered-surface coverage describes only its concrete manifest, not closure of the Territory domain. Production remains disabled.

Final local provider verification: Java 21 full compile (three inherited warnings); TerritoryWeaverProjection 86 assertions, TerritoryCanonicalMutation 83, TerritoryProtectionPolicy 419, and FactionWeaverProjection 101 passed. Architecture suite: four tests passed; coverage: zero audit errors; consistency: zero FAIL/WARN. The named new provider suite includes the final mode-aware catalog/import checks. New-head CI remains required.

## Canonical provider adapter checkpoint

Exact base: `85e6c45f232e7980799e60ae6f11d8492d47ac8e`, tree `4984f8a0a3d089492e65381afb4ff81826973558`. Its WorldWeaver verification `34152224791`, resource pack `34152225075`, Trash hardening `34152224854`, and docs inventory `34152224844` all succeeded. These runs cover the preceding projection provider, not the next canonical adapter commit.

The provider now exposes the exact design actions `territory.rename_canonical`, `territory.set_type_canonical`, `territory.set_owner_canonical`, and `territory.set_y_bounds_canonical`. Native territory-type and owner enums back typed catalogs. All four actions require ONE_SHOT / CANONICAL / LIVE_GM; generic primary-developer arming remains mandatory. Preparation retains only detached native values and produces no canonical write. The world owner validates the fresh snapshot; an AsyncIoOwner stage then invokes TerritoryManager.adjustConditionally only after the existing coordinator durably records PREPARED. Native admission rechecks authority and the canonical world fingerprint immediately before the atomic write. Capital collisions remain explicit native refusals, without hidden second-zone mutations.

Undo emits a new conditional native transaction; it preserves the original native receipt and records a new compensation receipt. Canonical before/after fingerprints and the actual mutated field are retained in the typed recovery plan. All value capture timestamps survive encoding, so reconstructed receipt equality remains exact. Recovery verifies the native operation ID, request fingerprint, before/after state, current zone, reverse field transformation and reconstructed world fingerprint. It never calls the mutation path. If the native store committed before the Weaver APPLIED write, recovery can reconstruct the Weaver receipt from actual native evidence. External drift or a corrupt inverse plan produces NEEDS_REVIEW and no fabricated committed receipt.

Verification: Java 21 full main/regression compile with three inherited warnings; TerritoryWeaverCanonical **94 assertions**, TerritoryWeaverProjection **86**, TerritoryCanonicalMutation **83**, TerritoryProtectionPolicy **419**, all passing. The canonical suite covers all four mutations/Undo, durable-PREPARED-before-I/O, SANDBOX refusal, stale snapshots, external Undo conflict, eight journal crash boundaries with unavailable-world wakeup, and corrupted inverse recovery. It compares actual native YAML bytes to prove recovery performs no domain rewrite. The suite is registered in Gradle check. Consistency: zero FAIL/WARN; architecture four tests pass; coverage 336 authorities / 55 domains / 51 blockers / zero audit errors; profile guard 688 classified / zero unknown, stale, invalid or transition entries. Main source count: 1248.

No new-head CI or connected runtime proof is claimed yet. Location/block trace frontend, complete Trash surface, later Fork controls and connected/client evidence remain required. Ambiguous entered-stage failures remain NEEDS_REVIEW under the existing durable coordinator and need the planned developer recovery controls; no force retry or automatic native replay is introduced. Production remains disabled.

## Local Subject inspection checkpoint

Exact base: `3af3bd0816793dd06ac22a2bd98684d411c72a13`, tree `9cc8e2143804c90b150565f325b83e3b298d5bd0`. Its WorldWeaver verification `34153385149`, resource pack `34153385053`, Trash hardening `34153385007`, and docs inventory `34153385156` all succeeded.

The same provider now contributes an inspect facet for PLAYER, ENTITY, BLOCK and LOCATION subjects. `TerritorySubjectInspection` resolves entity/player handles only inside their owner callback; coordinate subjects require the region owner and an already loaded chunk. It retains no live Bukkit object. The snapshot contains typed location, canonical zone ID/name/type/owner, shape/center/radius/Y-band/vertex count, and the ordered native decision/reason/trace for build, interact, PvP, explosions and fire. Canonical-zone and projection fingerprints are checked again after capture, rejecting observed drift. No chunk is loaded for inspection.

These traces explicitly identify their context: player actor at their own location, or environmental context for other local subjects; no PvP victim is invented. Victim-specific DOOM/combat-state inspection remains the native `tracePlayerDamage` API. World mutation actions remain WORLD-only; local discovery exposes inspection and does not offer mismatched canonical/projecting actions. Generic local-to-world navigation belongs to the remaining Subject/AREA UX work.

Verification: full Java 21 compile; new TerritorySubjectInspection suite **59 assertions**, canonical provider **94**, projection provider **86**, all pass. The new suite validates native pure-policy trace preservation, immutable typed location/facts, four local subject kinds, generic facet discovery without recapture, foreign-owner fixture refusal and incomplete/wilderness traces. Gradle check includes it. Architecture four tests pass; consistency zero FAIL/WARN; coverage 337 authorities / 55 domains / 51 blockers / zero audit errors; profile guard 688 classified / no unknown, stale, invalid or transition entries. Main source count: 1249. These are source/fixture checks; actual populated Paper/Folia and connected-client inspection remain evidence gates. No new-head CI result or full-domain completion is claimed yet.

Remaining work: full Trash surface; Territory Fork and generic navigation in their planned phases; connected runtime/client evidence; final universal coverage closure. Production remains disabled.


## Native Trash rule-field authority checkpoint

Exact base: `be23de1332814b91e97c4b33cdf2b116ac71754d`, tree `630b8c968f24c9e50fd307e50419644663f5af75`. Its WorldWeaver verification `34154553019`, resource pack `34154552965`, Trash hardening `34154552954`, and docs inventory `34154552893` all succeeded. These runs cover the preceding local Territory inspection checkpoint.

The normal Trash relic runtime now owns and uses one `TrashRuleFieldService` for field admission, geometry, exclusive claims, expiry and shutdown. Its immutable field records retain world UUID and finite coordinates instead of Bukkit Location/World objects. The existing four native field kinds, 32-per-world and 128-global caps remain; the shared service also bounds radius to 32 and lifetime to 60 seconds. Native defaults fit those bounds. Admission and claim contention serialize only bounded in-memory operations, with no I/O or Bukkit calls under the service monitor.

Conditional developer removal requires the observed service revision and an exact immutable field match, and cannot interrupt a native consumption claim. Expired claimed fields cannot authorize late inventory consumption; their reservation remains until the native continuation releases it, after which expiry returns it for owner cleanup. Shutdown drains all fields for reservation cleanup and closes subsequent admission. Native projectile-wall activation still obtains its real inventory reservation through the existing history route; this extraction introduces no synthetic reservation or developer prototype. Acoustic event handling uses the event location instead of reading another entity's live location.

Verification: full main/regression Java 21 compile succeeds with three inherited warnings. New `TrashRuleFieldRegressionSuite`: **182 assertions**, including four-kind geometry/world isolation, exact removal and revision conflict, immutable snapshots, expiry/claim cleanup, shutdown, invalid values, 32 concurrent claims and 32 concurrent admissions at capacity. Existing TrashRelic and TrashProductionHardening regressions pass. The new suite is a Gradle check dependency. Architecture: four tests pass; coverage: 338 authorities / 55 domains / 51 implementation blockers / zero audit errors; profile authority: 689 classified / zero unknown, stale, invalid or transition entries; consistency: zero FAIL/WARN. Source count: 1250 main Java files. New-head CI is still required.

This is the canonical native service seam required by design section 33, not a completed Trash provider or persistent rule-field Fork. Shared anomaly/relic activation APIs, developer history/prototype actions, durable provider mutation/recovery and influence admission remain implementation work. Existing projectile-wall cross-owner continuation and disable/unload recovery need hardening before exposing developer field creation; this change does not claim those paths safe. Connected Paper/Folia and client evidence remain required. No full-domain, merge-ready or production-ready verdict; production remains disabled.

## Trash smoke lifecycle repair — 2026-09-08

Continuation parent: `12c5195e0913d54b4232a5d31de6638136b6c4f8`, PR #159. Its current Trash workflow `34197600055` fails on both Paper (`101968709602`) and Folia (`101968709418`): the probe still reflectively reads fields removed by the native service extraction. Decoded logs confirm `IllegalStateException` followed by `MissingStartupProof`. WorldWeaver `34197600063`, resource-pack `34197600051` and docs `34197600045` succeed at this parent.

The probe now reads the assembled `TrashRelicRuntime` as its real type and captures its canonical `ruleFields().snapshot()`. Startup requires an open service with zero fields and claims; shutdown requires a closed service with zero fields and claims. Other native cleanup assertions and both workflow marker gates remain intact. Hidden payloads and exception messages are still excluded from logs. No duplicate registry, synthetic runtime service or production enable is introduced.

The field suite exercises the actual lifecycle diagnostic on a fresh service, admitted and claimed fields, and native shutdown; negative snapshot fixtures independently prove rejection of residual fields, residual claims and wrong open/closed state at each boundary. Native source contracts require both assembled service calls. Full Java 21 main/regression compilation passes (1434 sources, 49 real dependency jars, three inherited warnings); TrashRuleField passes 194 assertions and existing TrashRelic/TrashProductionHardening suites pass. Consistency is 0 FAIL / 0 WARN; architecture 4 tests pass; coverage remains 338 authorities / 55 domains / 51 blockers / 0 audit errors. Local wrapper distribution download is network-blocked and installed offline Gradle lacks the run-paper plugin; these preflight results do not replace the required new-head GitHub build and Paper/Folia smoke evidence.

Concurrent cumulative review: #155 advanced to `7cdea83a4785cfce26db0f7d854c6b124ee0706b`, changing only Pillow pixel access in `scripts/process_trash_sprite_sheets.py`. This stack already contains the independently verified histogram/getcolors correction and negative pixel fixtures from WW-00. The newer alternative does not change gameplay or require replacing that tested implementation. No historical/archive state was restored. The #155 docs job fails during temporary Git-directory cleanup (`OSError: Directory not empty`); that is separate from this branch's successful docs run and the confirmed Trash probe regression.


## Native Trash inspection checkpoint — 2026-09-08

Verified parent: `cf93e05847c6f02bdcf4f0fb4b18b7e785f15321`, PR #159. The first blocker is closed at that commit: Trash workflow `34200127389` succeeds, with both Paper and Folia startup PASS and SHUTDOWN_PASS markers read from job logs. WorldWeaver `34200127377`, resource pack `34200127361` and docs `34200127390` also succeed. These runs cover the lifecycle-probe repair, not the inspection changes below.

The native history and anomaly stores now use one explicit reentrant lock each in place of their intrinsic monitor. Existing read/write serialization and native WAL/YAML authority remain. The new `tryInspect` ports never wait for that lock, refuse reentrant access from an in-progress mutation, return detached native state, and distinguish unavailable state from known absence. They remain unavailable after an uncertain write or failed load until explicit native disk assessment. No copied registry or second history authority exists. Anomaly counters are explicitly runtime observations: unsaved WATCHED_TICKS are not represented as restart durability. Its durable mutation also restores working memory on CriticalPersistenceWriteError (an Error), preserving the original failure rather than exposing the unacknowledged candidate.

TrashHistoryService owns the read-only physical marker validation, including singleton/current revision, base/lifecycle consistency, origin and pending repair. Present-but-wrong-type or blank authority/origin/repair markers no longer masquerade as absent fresh state. The provider never reads or writes raw PDC. The existing archaeology fact engine now offers a pure detached-evidence overload; ordinary evaluation delegates to it. Developer inspection derives level-50 potential facts without calling discovery/progression awards or claiming the inspecting player's learned knowledge.

The composition registers TrashWeaverProvider for ITEM_SLOT and WORLD. Player-slot access is owner-checked and fingerprint-verified before native inspection. The generic frontend consumes detached facts, dynamic canonical identity and native rule-kind catalogs; no frontend/domain branch was added. All 64 retained history events and 64 bounded owners fit inspection limits. World views show actual native field coordinates, ownership, expiry and claim state, scoped to the requested world, without acquiring a claim or expiring a field. Separate native store observations are not an atomic cross-store transaction. No mutation uses these snapshots as admission evidence.

Only `trash.inspection_subset` is registered as read-only. The WW-00 `trash` domain remains DEFERRED_BLOCKER with its FULL_PROVIDER target unchanged; none of the 51 implementation blockers is waived. No action, import, export, Undo, recovery receipt or prototype is fabricated. Shared anomaly/relic activation, all seven developer actions, durable provider journal/recovery and influence admission remain implementation work. Developer wall creation remains forbidden pending the known projectile continuation/lifecycle/influence repair. Connected holder/GUI/client and populated cross-region field evidence is still required.

Local verification: full Java 21 main/regression compilation; native store inspection suite 37 assertions using real catalog, YAML and WAL, held-write concurrent reads, reentrant refusal, validation rollback, critical failure before/after replacement and reload; provider inspection suite 175 assertions covering generic discovery, catalog replacement/type invalidation, revoked authority, unsupported actions, maximum retained history/owners, native field claims/world isolation/expiry and immutable read behavior. Both are normal Gradle check dependencies. Existing Trash catalog, distribution (30 million events), history, anomaly, archaeology, relic, field (194 assertions) and hardening suites pass. The two source guards affected by the lock/writer refactor now verify the actual shared lock and production atomic writer, complemented by the new behavioral tests; no assertion gate was removed.

The opt-in Paper/Folia Trash probe now also checks real detached ItemStacks against isolated native history files: fresh stacks remain byte-identical without allocated history, tracked origin survives, stale revisions/duplicate stacks/untracked lifecycle phases/partial and wrong-type markers are refused, archaeology derives repaired evidence without item/WAL writes, and actual WAL reload preserves inspection. These checks are required before the existing startup PASS marker; they do not create fake gameplay entities or write synthetic history into the assembled gameplay store. New-head workflow results must still be checked before claiming these added native checks passed.

Inventory after reviewed source hashes and bootstrap assignment: 339 authorities, 55 domains, 51 blockers, zero audit errors. Architecture has four passing tests; PlayerProfile authority has 692 classified findings with zero unknown/stale/invalid/transition entries; consistency has zero FAIL/WARN. Main source count is 1251. Production execution and artifact gates remain disabled. Full WW-05, complete design implementation, connected playtest and production acceptance are not established.


Exact inspection source result: `8a9bc6647abdca508b359041d002bba6b95f1387` (tree `180bfc69fd228cba979f8c00b517c0efd977465f`) passes all four workflows: WorldWeaver `34202573875`, Trash `34202573908`, resource pack `34202573818`, docs `34202573873`. Full build job `101984541099` logs show both new regression tasks (37/175 assertions), :build and BUILD SUCCESSFUL. Paper Trash `101984541057` and Folia Trash `101984541075` logs show startup PASS after the added detached-item checks, actual disable, shutdown PASS and BUILD SUCCESSFUL. This closes the newly added detached-item/native-history smoke scope; connected players/GUI, multiplayer and populated cross-region evidence remain required.

Final cumulative freshness review: #155 advanced again to `0cd465639e73f3c2e8dee1cfe107c97f010d1f8d`. Its three-file diff adds local maintenance/gc suppression only inside temporary inventory test repositories and aligns ROADMAP with the approved tax-free fresh start while retaining paid faction-switch WAL failure/recovery evidence. No Java gameplay changes occur. These additional sibling edits are reviewed but not ported to this WW-05 branch; future cumulative refresh must preserve that later explicit decision, strict fixtures and public-guide mirror policy. The older Pillow alternative remains redundant with this stack's verified implementation. No merge, reset or sibling rewrite occurred.

## Native wall safety admission and uncertain-write fences — 2026-09-08 continuation

The public starting HEAD was `63d401eb55f726310f2e4fb2bb2825ef34c89f89`, tree `e4155759da2a3a5f660f544cda632e92708504a8`. All four workflows on that exact predecessor completed successfully: WorldWeaver `34203399177`, Trash `34203399172`, resource pack `34203399287`, docs `34203399235`. The previous pending-docs status is superseded. All eight stack PRs and #155 `0cd4656` were rechecked; none advanced or merged. The old worktree's Git metadata disappeared, so the active feature branch was freshly cloned from public HEAD into `/workspace/scratch/bbb9a568eb4c/IceSMP-continuation`. Its instructions/design/evidence matched the already fully read sources. Historical directories and branches were not reconstructed over current work.

The first finding has a conservative native safety repair, **not complete cross-region capability**. `TrashRelicRuntime.onProjectileLaunch` no longer zeroes velocity or passes Projectile/Vector state into an inventory-owner continuation. Its actual native completion path reacquires the reservation owner by UUID, verifies current ownership of both entities before accessing player inventory/state, requires online/same-world/current field claim and open tracking admission, commits the exact reserved brick through the existing native history transaction, then removes the projectile in that same owner callback. No projectile is mutated before consumption acknowledgement. Missing/foreign-region/offline/world-mismatched owners are refused without consuming the brick or altering projectile physics. In particular, a wall whose owner has moved into another Folia region will currently **not intercept** that projectile; do not report this as complete wall behavior or silently remove this limitation from acceptance.

The old detached freeze/removal handoff is removed, not made recoverable. Recoverable cross-region consumption/removal, crash or removal failure after acknowledged consumption, shutdown concurrent with an already admitted native commit, and full native influence admission remain implementation work. A durable receipt must belong to the canonical native activation/history seam, with detached operation identity and real owner/lifecycle recovery; a fixture-only state machine or a second field/history authority is insufficient. Developer wall creation remains unregistered. This safety restriction must not be mistaken for a normative design change or a waiver of cross-region semantics.

Task capacity now has one bounded, in-memory native admission object inside `TrashRelicPolicy`, used by the real runtime. It holds only identity tickets, has a 256-task cap and closed lifecycle, and rejects duplicate entity admissions. Identity equality prevents an old retired callback from releasing a later admission of the same UUID. Null scheduler return, scheduling exception, retired callback, timeout, successful completion, tick exception and shutdown release or invalidate admission. Field claims release in `finally`, including critical persistence errors. No ticket retains Bukkit entities, ItemStacks, vectors or field authority. The actual Paper/Folia probe reads typed startup/shutdown tracking snapshots and requires the correct lifecycle, zero active tickets and the unchanged cap; negative tests reject each incorrect state instead of retaining stale reflective field names.

World unload removes native fields and releases reservations through owner-correct cleanup. Player cleanup removes native wall authority before clearing physical reservation markers. Startup/quit/expiry/shutdown marker cleanup checks current entity ownership or schedules to the owner; rejected work after plugin disable leaves a non-authoritative marker for existing join/start/use stale-marker reconciliation. A marker-cleanup RuntimeException after a successful history transform is recorded in aggregate but cannot turn the acknowledged consumption into a retry. No new public commands, permissions, metadata authority, natural history events or client surfaces were added.

A second confirmed root cause was that the inspection fence did not fence writes: after an uncertain history append or anomaly atomic replacement, rolled-back working memory could still be mutated/saved, potentially erasing the durable candidate or appending a duplicate sequence. Native `TrashHistoryStore.transact/save` and `TrashAnomalyStateStore.add/addDurably/save` now refuse unloaded/failed/uncertain state before mutation or I/O. Only a successful native load assessment reopens them. Their existing native WAL/YAML, rollback and critical errors remain unchanged. A package-native journal appender seam delegates production writes to `TrashHistoryJournal.append`; fault injection writes and fsyncs the real WAL before throwing, rather than simulating a durable receipt. Tests prove that subsequent mutation/save cannot touch disk, load sees the actual event once without replay, and later acknowledged append/compaction reloads consistently. Anomaly tests cover failures before and after actual atomic replacement and reject both volatile additions and durable saves until assessment. Existing mutation methods still hold native locks during their I/O; this does not establish nonblocking activation or asynchronous owner-safe inventory projection.

The exact two temporary-Git-fixture maintenance/gc fixes from #155 `0cd4656` are now ported into `scripts/tests/test_inventory.py` and `test_release_inventory.py`. All assertions and strict cleanup remain. The third ROADMAP-only change is still not ported; the later explicit tax-free fresh-start decision remains authoritative, and a future public-doc change requires its normal mirror update. This supersedes only the earlier statement that all three sibling edits were unported.

Local validation: all 1437 main/regression Java sources compile against 49 actual Java 21 dependencies with zero errors and three inherited warnings; all nine native Trash suites plus the provider suite pass, including 30M distribution events. Native inspection/durability assertions increase from 37 to 65; provider inspection remains 175; field authority remains 194. The relic suite adds concurrency, duplicate/late-retirement, close/admit, failure-order and negative runtime-probe checks. The two upstream fixture suites pass 13 tests; resource-pack helpers pass 18 tests and sprite constraints pass 6. Coverage remains 339 authorities / 55 domains / **51 implementation blockers** / zero audit errors; profile authority is 694 classified / zero unknown, stale, invalid or transition findings; architecture four tests and consistency zero FAIL/WARN pass. No new top-level Java source was added (1251 main files).

Local wrapper distribution access is blocked; the installed Gradle 9.4.1 offline build cannot resolve uncached run-paper 3.0.2. Full new-head Gradle and native Paper/Folia results must therefore be verified on GitHub, not inferred from javac or the successful predecessor. Runtime startup/shutdown probes exercise assembled empty native state and detached item fixtures, not connected-player projectile interception, region transitions, world unload under traffic, multiplayer or human playtest. Those remain explicit evidence gates for implemented paths; missing cross-region recovery and influence remain implementation blockers. The full WW-00–WW-09 scope, production-disabled gates and no-merge/no-release constraints remain unchanged.

Published safety/fence checkpoint: `60da0dff2d4dc03c3c0732354fc5502df608255a`, tree `1714203ea33a85cc35fb127dfda35cdfc81ad0cb`. All four exact-head workflows succeed: WorldWeaver `34268750955`, Trash `34268751187`, resource pack `34268751044`, docs `34268750957`. Decoded full-build job `102204690716` runs the strengthened relic suite, native inspection 65, provider inspection 175 and completes BUILD SUCCESSFUL. Actual native Trash Folia job `102204691106` and Paper job `102204691254` both emit startup PASS, disable, shutdown PASS and BUILD SUCCESSFUL. This proves the added assembled empty-tracker lifecycle checks, with the runtime-scope limits above.

## Native wall transaction admission — follow-up

`TrashHistoryStore.tryTransact` now acquires the same canonical native lock with immediate `tryLock`, refusing contention and reentrant entry without evaluating admission, projecting items or entering a second transaction. It requires acknowledged loaded state, checks the supplied live admission before any compaction, then delegates to the unchanged native `transact` rollback/WAL path and rechecks admission after possible compaction before mutation. It holds no second authority, queue or history cache. This is non-waiting **admission against another writer**, not asynchronous I/O: an admitted transaction still performs native WAL/compaction work synchronously.

`TrashHistoryService.tryTransformInventorySlotOnSuccess` reuses the same exact-slot item/history projection and rollback body as the ordinary method. The actual native wall consumer calls this port with its current tracking/owner/online/world/field-claim predicate. It therefore cannot wait behind another owner transaction and then consume using the pre-wait lifecycle decision. This does not create a durable cross-region consume/remove receipt or solve every race after final admission; those remain the next wall implementation obligations before any developer action.

Native inspection/durability tests now pass **81 assertions**. They hold an actual native writer and require immediate refusal with untouched callbacks, reject reentrant and final stale admission, preserve original projection failure and item/history rollback, fence unloaded/uncertain try-writes, and generate 1024 real fsynced WAL records to reach native compaction. The admission predicate changes when the real snapshot appears; the post-compaction check refuses the candidate while exact acknowledged history and empty compacted WAL reload correctly. Full 1437-source Java 21 compile is clean apart from the same three inherited warnings; affected history/relic/hardening/field/provider suites pass. Inventory, architecture, profile guard (694) and consistency remain unchanged and pass. New-head GitHub results must still be checked separately from `60da0df`.

## User-reported development-server findings — explicitly deferred

During this continuation the user supplied actual Folia logs: `/hud` reached `PlayerProfileAuthority.current()` after the authority was uninstalled; tab completion later reached a closed plugin JAR (`zip file closed`). Subsequent logs explicitly identify the disable trigger: the required FancyNpcs authored snapshot had **18 required, 0 available, 18 missing/mismatched, 0 lookup errors**, followed by the existing fail-closed disable. Deleting playerdata cannot supply those NPCs. No builder world coordinates, NPC bindings or evidence were fabricated, and the gate was not weakened.

Source review confirms that BasicCommand handlers are registered directly and have no shared closed-lifecycle admission for execute/canUse/suggest. `ResourcePackListener.close()` clears server-side capability state and its development HTTP/task resources but does not remove the owned resource-pack layer from connected clients. The logged native-HUD-fallback message is not proof that vanilla client HUD was restored; the user observed that it was not. Owner-correct cleanup before live disable and late command/classloader protection remain unimplemented. A provisional untested command guard was fully reverted after the user explicitly said to leave this investigation for now and continue WW blockers. **No HUD/command/disable fix was published. Do not resume this side investigation ahead of WW without new user steering.**

A later user `/icesmp reload` log reports authored Equipment 2.0 DISFAVORED_STAT/CLOTH_ARMOR_OUTLIER warnings and `Missing packaged authority: content/progression/classes.yml`; reload correctly refused the candidate and retained the prior snapshot. That resource exists in current source and is not excluded by processResources. A closed loader is consistent with the earlier failure, but the actual deployed JAR/hash was not inspected, so an artifact problem is not ruled out. `/pl` listing the plugin does not prove its enabled state. These observations are unresolved evidence/findings, not Trash causality, a successful reload, or permission to mute equipment audits. The user-reported environment has no complete NPC readiness and is not runtime/playtest PASS for WorldWeaver.


## Native pending projectile-wall receipt checkpoint — 2026-09-08

The user resumed the previously deferred lifecycle findings. They are repaired in published `ba830e7`; exact command-inventory scanning was corrected in `34549e7`. All four workflows on `34549e7` succeed, with actual real Paper/Folia reload/direct/cooperative shutdown logs read. Zero connected players remain an explicit evidence limit. The primary resume's latest continuation section records exact runs/jobs and the published IceSMPGuides #20 mirror. Old deferral and unported ROADMAP statements above are historical.

The native TrashHistoryStore transaction now records the exact consumed brick and pending projectile removal in the same WAL acknowledgement. The receipt is keyed by the existing field operation UUID and copies the actual native field definition/reservation plus actor, world, projectile, instance and before/after revisions. No vendor receipt, second ledger or developer mutation framework was reused to fabricate effect proof. Snapshot schema 4 and WAL payload schema 2 retain read support for legacy 3/1 while fencing older binaries against silently discarding new receipts. Strict receipt/history validation, bounded 1024 pending capacity, no eviction, and pending-instance mutation fences survive actual restart and compaction.

The existing co-owned runtime uses this native transaction, rechecks field/owner/lifecycle admission after acknowledgement, and confirms removal only after the projectile's owner observes its actual remove result. A missing UUID is not completion evidence. Rejected/busy confirmation, expired or closed admission and failed physical removal leave a durable pending operation. Native item inspection pairs history/receipt under one lock; hidden world inspection shows a world-filtered bounded list with exact total, explicit truncation and no effect-completion claim. This is a read-only provider extension, not full Trash action coverage.

Local Java 21 preflight compiles 1441 sources against 49 real dependencies (three inherited warnings, zero errors). The existing native inspection task includes 69 new real native WAL/storage assertions, with its original 81 preserved; provider 248 and field 194 assertions pass. All other Trash suites, including the 30,000,000-event native distribution run, pass. The telemetry guard now checks every existing rejection catch body independently and separately requires unacknowledged-effect telemetry; it no longer wrongly assumes that only exceptions can increment errors. Exact successor CI is still required after publication.

Still implementation work: native observed recovery and inventory/playerdata reconciliation; complete cross-region owner execution without frozen foreign handles; uncertainty after acknowledged consumption, retirement/unload/disable closure, native influence admission; shared anomaly/relic activation APIs; all seven developer mutation actions and durable adapter/Undo/recovery/prototype integration. The co-owned safety restriction remains. No developer wall action, full provider, zero-leakage or runtime client PASS is claimed. Coverage remains 339 authorities, 55 domains and 51 implementation blockers.


## Native claim identity continuation

Pending-receipt source `831a5f1edafc7835004701fec4625ec06bc2626e` (tree `dcbbfa51c3399d191381b3573e0debfe207399ca`) passes WorldWeaver `34284616348`, Trash `34284616414` and resource pack `34284616433`; Docs `34284616432` also succeeds. Decoded job `102257189349` confirms exact HEAD, consistency 0 FAIL/0 WARN, 44 tooling tests and zero report-mode blocking findings (69 roots, 303 routes, 79 root aliases, 99 routing aliases, 140 features). Decoded build job `102257189021` confirms receipt 69/native inspection 81/provider 248 assertions and BUILD SUCCESSFUL. Actual four Trash Paper/Folia direct/cooperative logs `102257189126`, `102257189312`, `102257189348`, `102257189624` show reload/startup/shutdown PASS and BUILD SUCCESSFUL; cooperative logs explicitly record zero connected players. This is not connected wall recovery evidence.

The existing native field-claim registry now issues identity tokens bound to the exact immutable field. Runtime admission, release and consumed-field cleanup require that same token. A delayed callback cannot release or remove a subsequent claim, including an identically re-added field; a token from another service instance is refused. This replaces the old UUID-only claim set in the canonical service, without creating a second authority or changing snapshot field/claim inventories. Existing co-owned runtime and read-only provider behavior remain the active routes.

Native field regression now has 235 assertions, including concurrent delayed release/removal, re-claim, re-add, foreign-service token, expiry and closed-authority cases. All 1441 Java sources compile and the affected relic/hardening/provider suites pass. Profile authority has 697 classified findings and no transition after explicit world-operation classification. Coverage stays 339 authorities/55 domains/51 blockers. Cross-region execution, pending-operation recovery and native influence remain the next implementation work; no full-wall or full-provider completion claim is made.


## Pending-consumption physical projection recovery

Exact-claim source `8ee1dc6555ec8f790297877b652ccd44780d0e03` is published. Its WorldWeaver `34286054024`, Trash `34286054098` and resource pack `34286054042` succeed; Docs `34286054060` also succeeds; decoded job `102261799048` confirms exact HEAD, consistency 0 FAIL/0 WARN, 44 tooling tests and zero report-mode blocking inventory findings. Actual build and four native Paper/Folia direct/cooperative logs were decoded, including field 235/receipt 69/native inspection 81/provider 248. These do not establish connected inventory/region evidence.

The native history transaction can now conditionally reproject an already acknowledged **pending** wall consume to an existing stale singleton. It checks exact receipt, actor, before instance/revision/phase and final owner admission, applies the existing authored phase/factory plus acknowledged native revision, and restores the old physical projection on failure. It does not append history/WAL, create a missing item, refund a consume or replay projectile removal. Native owner cleanup indexes physical inventory once, rejects locally duplicated instances and checks the exact reservation token and unchanged slot snapshot. Unknown or pending native state preserves markers instead of silently deleting recovery evidence. Startup/join and later brick use invoke the same native path.

The real native store suite now has 82 receipt/recovery assertions, with 81 original inspection assertions preserved; fields 235/provider 248 and all affected suites compile and pass. The existing opt-in Paper/Folia probe adds real detached ItemStacks over isolated native WAL files, including rollback/reload/repeat/refusal and unchanged-WAL checks. This is a stale physical snapshot fixture and needs exact new-head CI execution; it is not a connected playerdata-save test or proof of globally unique physical custody.

A further completed-operation gap remains: observed removal currently retires its receipt before a durable Minecraft inventory-save acknowledgement. After a crash, already-confirmed operations can therefore lack the receipt needed to assess old physical playerdata. First next implementation is bounded native observed-completion retention and justified retirement/recovery, followed by busy confirmation recovery, cross-region execution and native influence. No automatic save proof, refund/replay, developer wall action or complete recovery verdict is introduced. Full WW scope and 51 implementation blockers are unchanged.


## Retained observed wall recovery and bounded acknowledgement

This section supersedes current-state statements in all chronological checkpoints below. Public parent is `204cbfaade47d39ea2cf8833f0a7114ca251d5b9`, tree `8facc7cbf3a4d1bcd92541e45cd687d84fbc69f7`, on `feature/world-weaver-ww05-trash-territory`, open draft #159 based on unchanged #158. Resolve this successor's actual remote HEAD before resuming; never reset to a recorded parent. The complete WW00–WW09 user request is still active, with commit/push authorized, no merge/release/production enable.

All four exact-parent workflows succeed: WorldWeaver `34287911330`, Trash `34287911298`, resource pack `34287911282`, Docs `34287911290`. Decoded build job `102267647104` confirms BUILD SUCCESSFUL, receipt 82 / native inspection 81 / fields 235 / provider 248. Decoded actual Trash logs `102267647286`, `102267647447`, `102267647451`, `102267647472` confirm Paper/Folia direct/cooperative reload, startup and shutdown; cooperative cases have zero connected players. These execute the detached stale-item projection fixture, not connected playerdata durability or projectile interception. Decoded Docs job `102267647551` confirms 44 tests, consistency 0 FAIL/0 WARN and zero report-mode blocking findings. The seven stacked parent PRs were rechecked unchanged; the parent table later in this document remains valid for #152–#158. IceSMPGuides #20 remains at `27284ee93864c3f1429f79dcbf22d4ef0163a165`, with all 14 public documents byte-identical.

The completed-operation retention gap identified at `204cbfa` is now implemented in source. Native history snapshot schema 5 reads 3/4/5; WAL payload schema 3 reads 1/2/3. Positive removal acknowledgement changes the exact receipt to immutable `removalObserved=true` instead of deleting it. Observed receipts remain within the existing 100000-instance native history lifetime; unresolved capacity remains 1024 with no eviction. A derived instance-to-operation index and unresolved ID set are rebuilt from the same canonical receipts under the same transaction/load/rollback lock. They are not a second authority. Legacy schema-2 explicit completion records retain their existing receipt as observed; an already compacted legacy snapshot with no receipt cannot be reconstructed from absence.

A bounded owner-captured query (at most 64 inventory instance IDs) exposes both pending and retained observed receipts to the existing recovery path. Normal pending inspection still lists only unresolved effects. Pending receipts continue to fence history mutation. Observed receipts permit later native history events and survive bounded event pruning; automatic old-item projection refuses if canonical history has advanced beyond the consumed revision. Exact actor, instance, phase, before revision, reservation token and unchanged locally unique owner slot remain required. Missing units, missing reservation evidence, duplicates and drift require review; nothing is created, refunded or replayed. A successful live ItemStack projection does not retire the native receipt and is not treated as Minecraft save proof.

Observed-removal acknowledgement now recognizes an exact already-durable receipt without another write or observer call. The live native path starts a bounded retry only after a positive owner-local `remove` observation. Up to 20 delayed attempts at 250 ms use the existing plugin AsyncScheduler with immutable receipt data; there is no entity/inventory access, second ledger, executor or effect replay in the callback. Closed runtime/plugin stops retries, scheduler rejection or exhausted retries preserves the pending receipt and aggregate telemetry. A process crash before observation reaches the native WAL remains genuinely unassessed.

Local evidence for this successor: full 1441-source Java 21 compilation, zero errors/three inherited warnings; native wall receipt/recovery regression 124 assertions, original native inspection 81, fields 235, provider 248 and all native Trash suites including 30M distribution pass. Schema/type/transition corruption, pre-observed insertion, observation rewrite/retirement, legacy migration, lost acknowledgement after real fsync, busy/reentrant refusal, retained projection, capacity and event-pruning cases use actual native files/WAL. Existing Paper/Folia probe is extended with detached observed-receipt recovery and exact idempotent acknowledgement; it needs successor CI before runtime PASS. A positive observation in that isolated storage fixture is explicitly fixture input, not a spawned projectile. Consistency 0/0, architecture four tests, Profile 698 classifications/zero transitions and coverage 339 authorities/55 domains/51 implementation blockers/zero audit errors pass. Exact successor CI remains required after publication.

**First next implementation work:** finish the native cross-region owner handoff and its lifecycle/retirement/expiry/recovery contracts using these exact receipts and claim identities. The current runtime still restricts interception to co-owned player/projectile entities; no projectile freeze or foreign inventory access may return. Native influence admission and genuine crash outcome assessment remain open. Do not register a developer wall action before that closure. Then extract canonical shared anomaly/relic activation APIs, implement all seven developer Trash actions and their durable adapter/Undo/prototype/influence integration, and finish WW04 universal ingress and full WW06–WW09. All 51 whole-domain implementation blockers remain; no full-wall/full-provider, merge-ready or production-ready claim is made.

Remaining external gates include the actual 18 authored NPC/world bindings, the user's deployed JAR/hash diagnosis, connected two-region HUD/pack/command shutdown, connected physical custody/playerdata/projectile tests, builder worlds, multiplayer and human acceptance. The already published lifecycle and inventory fixes are not to be redone or treated as deferred; historical deferral sections below were superseded by the user's instruction to fix findings first. Equipment audits and all fail-closed gates remain intact.

## Current continuation — native cross-owner wall handoff

This section supersedes older co-owned-only and absent-Relic-service statements below. Public parent is `9e910fd4486c84271c232de9f0097f47984c333f`, tree `0e58d2e731e72e8ca50e91f87d56937e26bbc398`, same feature/#159/base #158. All four exact-parent workflows succeed: WorldWeaver `34290555483`, Trash `34290555530`, resource pack `34290555494`, Docs `34290555497`. Actual decoded build job `102275912660` confirms receipt 124/native inspection 81/fields 235/provider 248 and BUILD SUCCESSFUL. Actual four Trash logs `102275912780`, `102275912816`, `102275912829`, `102275912878` confirm Paper/Folia direct/cooperative reload, startup and shutdown. The observed-retention detached-ItemStack fixture executed there; cooperative cases explicitly have zero connected players. Decoded Docs job `102275912481` confirms 44 tests, consistency 0/0 and zero report-mode blocking inventory findings. Resolve the successor's public SHA/CI before continuing; never reset to this parent.

The design-named native `TrashRelicActivationService` now coordinates the projectile-wall cross-owner path. This is a partial native Relic activation extraction, not the complete general activation API. It shares the existing TrashRuleFieldService, bounded projectile tracking and native history receipt; it has no independent registry, ledger, executor or mutation authority. Dispatch transfers the exact field claim/tracking ticket to the inventory owner, records consume+pending receipt through the existing native transaction, then schedules the projectile owner. The final projectile callback rechecks lifecycle, exact claim, validity, world and current containment before removal. No projectile freeze, foreign inventory read, synthetic Bukkit event or effect-before-WAL path is introduced. The co-owned fast path is retained.

Native final effect admission and observation are serialized with claim release/close in TrashRuleFieldService. This bounded owner-local section contains no WAL or scheduler wait; acknowledgement happens afterward. Duplicate/late callbacks cannot consume or remove twice. Refusal before consumption releases the native claim/ticket without changing history. Expiry, owner retirement, scheduler failure or shutdown after acknowledgement retains the pending receipt and reports the unresolved effect. Removal-triggered scheduler retirement is fenced so an entered observation/finally boundary is not falsely reported as failure. Exact token cleanup cannot release a replacement claim; lost acknowledgements remain fenced until real WAL assessment. Missing/old physical custody evidence is not converted into a refund or success claim.

Local source evidence: all 1443 Java main/regression sources compile with zero errors and three inherited warnings. Native owner-handoff suite passes 204 assertions; receipt suite 124, original native inspection 81, native fields 243 and provider 248 pass, with affected relic/hardening suites. Tests use deterministic owner queues over actual native fields, tracking and fsynced history/WAL, including duplicate callbacks, stale claims, busy writers, scheduler rejection/exception, inventory/projectile retirement, position refusal, expiry, false removal observations, real lost WAL acknowledgement and shutdown while acknowledgement is held. Final field-observation tests serialize actual concurrent close versus an entered effect. These are not real Folia regions or connected playerdata/projectile evidence. The successor still needs its own full GitHub build and existing smoke runs; those smoke probes have no connected cross-owner interception. Config/content audit (24634 leaves, 160 armor, 89/61 PvE, 69 commands, zero generated gameplay), resource helper 18 cases, architecture four cases, consistency 0/0 and Profile 698/zero transitions pass. Coverage is now 340 authorities, 55 domains, 51 whole-domain implementation blockers and zero audit errors.

The duplicate-method consistency scanner now uses actual named/anonymous type-body scopes rather than the nearest preceding type declaration. Six focused tests preserve true duplicate detection (including outer methods after nested types) while accepting independent native owner adapters. The existing Java scanner is extended; no gate or authority is bypassed. The public architecture inventory is 43 Trash classes and records the new native coordination component under the existing history feature; the IceSMPGuides architecture file is mirrored byte-identically in published `c782fc2e953e77b1de0b1f726d59cacf5cf05e29` (tree `01fd4f5bd55f05beacd3da6e288492dd3ccb1ae4`), same feature and draft #20.

**First next implementation work:** native Trash influence/prototype admission and propagation across actual activation, rule fields, item consequences and rewards, extending existing WorldWeaver influence/receipt seams. Review all retained uncertainty paths and physical-before marker limits; do not infer Minecraft durable saves or replay/refund missing outcomes. Then complete the shared Relic activation API beyond wall coordination, extract TrashAnomalyActivationService, implement all seven developer Trash actions and their journal/Undo/recovery/prototype contracts, and finish WW04 universal ingress plus full WW06–WW09. Developer mutation/wall actions remain unregistered; full Trash coverage and the 51 implementation blockers are not closed by this native handoff. Full Folia cross-owner runtime, connected physical custody/save, builder/NPC, multiplayer/HUD and human acceptance remain evidence-required. No merge, release or production-enable verdict is authorized.

## Native planned-unit admission and current CI correction — 2026-09-09

Current source parent is `28c21e9d3ecdbf51d14143fbeb25eb08ce9cd80a`, tree `4069f5d7ebbea80c2c02cb84a98fd0d89688c77f`, same feature/#159/base #158. The parent's WW workflow `34356041309` failed only at resourcePackRegressionTest: decoded job `102480843168` reports the obsolete assertion "NPC readiness gate was bypassed". ResourcePackDeliveryRegressionSuite still required shutdown for an unhealthy authored NPC snapshot despite the owner's explicit nonfatal-binding decision. The assertion now requires retained diagnostics and absence of NPC-driven disable; real cooperative owner-cleanup/profile-teardown assertions and the separate required FancyNpcs plugin/API contract remain. ResourcePackRegressionSuite passes (delivery 31); the NPC binding decision is not reverted. Parent Trash `34356041216` and resource `34356041270` succeeded; resolve Docs `34356041215` and successor CI separately.

The existing TrashHistoryService now prepares an opaque UnitPlan with the actual future native UUID before physical/history mutation. Preparation validates the actual singleton, canonical content, origin and current history, refuses pending walls or unavailable native state, and stores only a detached before-item plus authority-bound single-use state. There is no pending-plan registry, ledger or persisted second identity. Plans expire after five seconds; the commit rechecks exact item and native history under the existing writer lock. Fresh targets must remain absent; tracked targets retain their real UUID/revision. A different service cannot consume the plan. The native hand-individualization path uses the same plan and preserves the actual slot/hand, full inventory remainder and rollback contract.

TrashHistoryStore has a final-admission overload: repeatable freshness is checked before and after actual compaction; a single-use final permit is claimed exactly once immediately before native mutation. Busy/reentrant/initial or second freshness rejection does not spend the permit. Final refusal writes no event and a consumed plan cannot retry after refusal, rollback or lost acknowledgement. Existing native WAL, origin-event ordering, history capacity, pending-wall fences and uncertainty semantics remain authoritative; no schema migration is added. The generic neutral GameplayEffectPermit is exercised by regressions; full GameplayEffectGate/journal wiring is not yet implemented by this preparatory seam.

Fresh local evidence: all 1443 main/regression Java sources compile with zero errors/three inherited warnings. Native inspection suite now has 97 assertions plus preserved receipt124/handoff204 suites, using actual native WAL/fsync, held writers, real 1024-record compaction, single-use permit rejection/replay and lost acknowledgement. Existing history/relic/production hardening, resource-pack and NPC regressions pass. The real Paper/Folia native probe adds detached ItemStack tests for side-effect-free target preparation, source/authority/history drift, final refusal/no retry, exact origin/UUID, rollback and WAL reload. Those new ItemStack assertions require successor runtime CI; they are not connected inventory/playerdata evidence.

**First next implementation task:** bind these actual planned target IDs into GameplayEffectGate/WeaverJournal before native wall/item creation. Capture immutable native item/player/world/location/field causal inputs on their owners, use the single-use final permit at the new native boundary, and bound pending activation in the existing native activation/field authority. Complete field and interception consequence propagation as well as prototype custody; do not claim this planning API alone propagates influence. Shared Relic activation is still wall-only; Anomaly activation and all seven developer Trash actions remain absent. WW04 universal ingress and full WW06–WW09/51 whole-domain blockers remain. Keep developer mutations and production gates disabled until their documented contracts close. Missing NPC bindings remain diagnostic only by explicit owner instruction.

## Current continuation — native wall creation influence admission (2026-09-09)

This is the current continuation and supersedes older statements that all native Trash influence wiring is absent. The primary handoff remains this document; WW-05-territory-policy.md is supporting evidence, coverage.json is the inventory, and design-v2.md remains normative. Resolve current public HEAD first. Source parent is `9932473940c6e228d0faf4f2567e7737f2a35cf8`, tree `7e03a1003fde98101d57a56001cc06e2ee8a9e65`, same feature/#159/base #158 `ac6e46eb96a6d4cd8b00e778afb775acdeb2ae4c`. Do not reset to the parent. All four parent workflows succeed: WorldWeaver `34358087268`, Trash `34358086943`, resource pack `34358086999`, Docs `34358087036`. Actual build job `102487775521` and all four Trash jobs `102487763901`, `102487763913`, `102487763975`, `102487764042` were decoded: full build and Paper/Folia direct/cooperative reload/startup/shutdown pass. The planned-unit detached ItemStack probe executed there. Cooperative cases have zero connected players; no connected wall interception or durable Minecraft playerdata claim follows.

Decoded parent Docs job `102487763428` passes 50 tooling tests and consistency 0/0, but reports 70 inventory FAIL findings in report mode. These are now corrected at their cause: the owner-requested NPC callback change shifted 68 actual registrations by exactly six lines and changed both reviewed core-source digests. Names, implementation classes, aliases, routes, permissions and counts are unchanged. The exact manifest retains all gates and updates those source locations/digests plus its entry digest. A stricter local run confirms these 70 command/permission findings are gone, but fails on 249 separate UNDOCUMENTED_INVENTORY_ITEM component entries across the cumulative source. These remain genuine strict-release inventory work; report-mode success must not be represented as strict acceptance. Do not describe the parent's green Docs workflow as a zero-finding inventory result. The old `28c21e9` WorldWeaver failure was the obsolete NPC-shutdown assertion; `9932473` already repairs it while preserving actual shutdown cleanup/authority tests.

Native projectile-wall creation now captures real owner-local item/player/world/location sources and uses the actual native UnitPlan UUID plus the actual `trash.rule_field` Event UUID as influence targets before history or physical mutation. Both targets use the existing monotonic journal lineage. A bounded preparation is reserved in the existing TrashRuleFieldService before asynchronous journal admission, shares the existing 32/world and 128/global caps, and remains invisible to effect consumers. No parallel ledger, native identity authority, synthetic event, executor or developer action registry is introduced. The provider exposes pending preparations read-only, world-filtered, without a reservation token or a fake active-field claim.

The actual owner callback rechecks lifecycle, ownership, online/world identity, exact hand/slot/full source stack and unexpired preparation. Native history claims a single-use permit immediately before its WAL transaction and stamps the real reservation token inside the existing inventory projection/rollback boundary. Only after native WAL acknowledgement does a second single-use permit recheck freshly captured influence sources and activate the exact preparation. New influence, expiry, source drift, retirement, rejection or shutdown cannot activate a stale field. After acknowledged history but rejected field publication, no active effect is created; the reservation is cleared without inventing a refund or rolling back acknowledged history. Pending preparations are cancelled on owner cleanup, world unload, expiry and close; old/foreign callbacks cannot release replacements or active fields.

Local source verification: all 1443 main/regression Java sources compile with zero errors and three inherited warnings. Native field suite 294, influence propagation 226, provider inspection 257, native inspection 97, retained receipts 124 and owner handoff 204 assertions pass. Actual journal/storage fixtures verify durable original lineage on item/field targets before publication, retained lineage after removal, refusal of post-WAL source changes and single-use admission; field fixtures cover immutable snapshots, inactive preparations, mixed/concurrent/world/global capacity, stale/foreign identities and lifecycle cleanup. These are source/native-storage fixtures, not connected Folia players. Profile guard is 699 classified findings/zero unknown or transition, coverage 340 authorities/55 domains/51 whole-domain implementation blockers/zero errors; architecture four tests pass. Successor full CI must be resolved separately from the parent.

**First next task:** carry native field/item/player/world/projectile influence through actual cross-owner wall interception and both acknowledged consumption and final projectile consequences, with fresh owner admission at each effect boundary. Current wiring covers projectile-wall CREATION only; other field kinds and the remaining native relic/anomaly behavior paths are still not closed. Extend the existing TrashRelicActivationService/history/field and WorldWeaver journal seams; do not claim complete Trash influence or prototype custody. General Relic activation, TrashAnomalyActivationService, all seven developer Trash actions, prototype custody, universal WW04 ingress and WW06–WW09 remain unfinished. All 51 whole-domain blockers remain. Real cross-region Folia, connected custody/playerdata, builder/world/NPC content, multiplayer/HUD and human acceptance are evidence-required. Missing NPC bindings remain NONFATAL by explicit owner instruction; required FancyNpcs plugin/API initialization remains distinct. Commit/push on this feature is authorized; no merge/release/production enable.

## Native wall interception influence admission (2026-09-09)

This is the current handoff and supersedes older creation-only and ungated-interception statements below. Source parent: `ab4efb2d87e2afde08007eb585731c8cf62d48a3`, tree `cf3f20e3d54ab426186e4922119b610bd8fa91c8`; same feature/#159, unchanged base #158 `ac6e46eb96a6d4cd8b00e778afb775acdeb2ae4c`. Resolve the live successor HEAD before resuming. All four parent workflows succeed: WorldWeaver `34360383770`, Trash `34360383491`, resource pack `34360383806`, Docs `34360383496`. Decoded parent build job `102495539657` confirms full build success; this evidence belongs to the parent. The primary resume, normative design and coverage authority order remain unchanged.

Both co-owned and cross-owner projectile-wall interception now use the existing `TrashRelicActivationService`; the old direct consuming/removal fallback is removed. On the inventory owner, the runtime prepares two distinct single-use `GameplayEffectPermit`s through the existing journal, using actual field Event, native item UUID, player, world/location and projectile causal sources. The actual item and projectile receive durable original lineage before native consumption. The native history writer claims its consumption permit at final admission after repeatable freshness/compaction checks. Only an acknowledged native consume+receipt may schedule final projectile admission, which rechecks fresh owner-local projectile/field sources before actual observed removal. Field/item/source drift or policy-binding retirement refuses the next consequence. No second field/history authority, synthetic event, effect replay, fabricated completion or refund is introduced.

Preparation holds the existing exact field claim and tracking ticket, with a cancellable five-second deadline. If deadline/retirement arrives during an entered native WAL acknowledgement, its claim/capacity stays held until that acknowledgement returns. After an acknowledged consumption but refused removal, the real pending native receipt survives restart. Deadline registration races cancel exactly once; synchronous/asynchronous preparation failures report through existing aggregate telemetry and release their claim without gameplay mutation. Named owner ports and the launch-tracking callback retain only detached data and scheduler handles; UUID lookup and ownership checks precede live entity reads. Wall creation now similarly carries serialized before-item data and UUID across journal callbacks and re-resolves the owner before native admission.

Final local source evidence: all 1443 main/regression Java 21 sources compile against the 49 real cached CI dependencies, zero errors/three inherited warnings. Owner handoff **575**, retained receipts **124**, native inspection **97**, fields **294**, provider inspection **257**, influence propagation **227** assertions pass, plus native history/relic/production hardening suites. New tests exercise actual native WAL/fsync and WeaverJournal lineage/restart with deterministic owner queues, including deadline during held acknowledgement, delayed preparation, stale owner sources, new field influence before/after consumption, fresh projectile influence and retired policy binding. Source guards follow the common native path and retain positive-observation/aggregate-error checks. These are not connected Folia regions, live inventory/playerdata durability or populated interception evidence. Native history WAL remains synchronous; this checkpoint does not claim a fully nonblocking general activation API. The local Gradle distribution download is unavailable; the exact successor still requires its own full GitHub build and runtime smoke logs.

Coverage is 340 authorities / 55 domains / **51 whole-domain implementation blockers** / zero audit errors; its stage now correctly names WW05. Only reviewed native source digests change. Profile authority guard: 698 classified, zero unknown/stale/invalid/transition findings; four architecture tests and consistency/mirror checks pass. Public documentation is unchanged. The 249 strict inventory component findings recorded below remain open; report-mode Docs success is not strict release acceptance.

**First next implementation work:** extend native influence/prototype admission through the other rule-field kinds and remaining Relic/Anomaly consequences, using the existing journal and canonical authorities. Complete the general shared Relic API, `TrashAnomalyActivationService`, all seven developer Trash actions and durable adapter/Undo/recovery/prototype contracts, then universal WW04 ingress and full WW06–WW09. This wall-only continuation closes no whole-domain blocker. Connected cross-region Folia, physical custody/save/recovery, builder/world/NPC content, multiplayer/HUD and human acceptance remain evidence-required. Missing authored NPC bindings stay NONFATAL by explicit owner instruction; required FancyNpcs plugin/API initialization is separate. Commit/push on this feature is authorized; no merge, release or production enable.
