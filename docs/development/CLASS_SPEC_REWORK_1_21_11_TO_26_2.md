# PlayerProfile/class-spec 1.21.11 → 26.2 port contract

The domain remains Bukkit-independent and class/spec persists as the structured `class-spec` PlayerProfile section. Port validation must run the complete PlayerProfile regression set, authority guard and staging matrix. No ICS2 or independent ClassProfile migration path exists.
