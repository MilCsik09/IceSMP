package hu.taliann.icesmp.trash;

/** Bounded environmental/source context vocabulary used only for identity weighting. */
public enum TrashContext {
    WET,
    COLD,
    HOT,
    DEEP,
    UNDERGROUND,
    NETHER,
    OPEN_SKY,
    UNDEAD,
    HUMANOID,
    DARK
}
