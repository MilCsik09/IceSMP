package hu.taliann.icesmp.commands.currency;

import hu.taliann.icesmp.commands.Subcommand;

/** Currency subcommand marker; the contract lives in {@link Subcommand}. */
public interface CurrencySubcommand extends Subcommand {
}
