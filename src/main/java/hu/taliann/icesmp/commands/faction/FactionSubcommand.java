package hu.taliann.icesmp.commands.faction;

import hu.taliann.icesmp.commands.Subcommand;

/** Faction subcommand marker; the contract lives in {@link Subcommand}. */
public interface FactionSubcommand extends Subcommand {
}
