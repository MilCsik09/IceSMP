package hu.taliann.icesmp.commands.bank;

import hu.taliann.icesmp.commands.Subcommand;

/** Bank subcommand marker; the contract lives in {@link Subcommand}. */
public interface BankSubcommand extends Subcommand {
}
