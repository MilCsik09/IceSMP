package hu.taliann.icesmp.classspec.domain;

/** Durable final-trial state; encounter runtime is deliberately outside Profile v2. */
public enum CapstoneStatus {
    LOCKED,
    AVAILABLE,
    IN_PROGRESS,
    COMPLETED
}
